# 우완좌완 야구 매니저 — 작업 규칙

이 저장소는 **`index.html` 단일 파일**이다. 빌드 과정이 없다. 파일 하나가 곧 게임이다.
깃허브 페이지로 배포되고, 실제 사람들(팀원들)이 링크로 받아서 쓴다.
한 번 깨지면 여러 사람이 바로 겪는다. 그래서 아래를 지킨다.

---

## 패치할 때마다 반드시 (요청이 없어도 기본값)

### 1. 버전을 **두 곳** 같이 올린다

```js
const APP_VERSION = '2.6.0';                    // ① 여기
const APP_VERSION_NOTE = '한 줄 요약';           // ① 여기

const CHANGELOG=[
  {v:'2.6.0', t:'한 줄 요약', items:[            // ② 그리고 여기 (맨 위에 추가)
    '바뀐 것 1',
    '바뀐 것 2',
  ]},
  ...
```

`CHANGELOG` 는 업데이트 안내 카드에서 **이안·지우가 읽어주는 내용**이다.
빠뜨리면 안내가 한 줄짜리로 빈약해진다.
`vernotetest.js` 가 `CHANGELOG[0].v === APP_VERSION` 을 검사하므로 까먹으면 테스트가 잡는다.

### 2. 커밋 전에 검사를 돌린다

```
node lintcheck.js          # 선언이 사라진 함수 · 중복 CSS · 중복 키 · 용량
node verify.js index.html  # 엔진 캘리브레이션 6개 지표
node soaktest.js           # 풀 시즌을 실제 UI 경로로 완주
```

여유가 있으면 기능별 테스트도 같이 (`boxtest` `feattest` `awardtest` `sorttest`
`nametest` `mgrtest` `kakaotest` `careertest` `compattest` `smoketest` …).

**확률 기반이라 가끔 실패하는 것** — 실패하면 3회 재실행해서 판단한다:
`advtest.js` `wltest.js` `recruittest.js` `dectest2.js` `playtest.js`

`playtest.js` 의 「정타 치고 볼넷·삼진 나는 일이 거의 없다」 는 새 게임 시드에
따라 6회 중 1~2회 실패한다 (v2.86.0 에서도 같은 비율이다 — 재본 값이다).
`dectest2.js` 의 「수비 판단창이 여러 번 떴다」 도 5회 중 3회 실패한다.
둘 다 원래부터 그렇다.

jsdom 테스트를 **여러 개 한꺼번에 돌리면** 부하 때문에 멀쩡한 것도 실패한다.
하나씩 다시 돌려서 판단해라.

3회 다 **같은 항목**이 실패하면 그건 간헐이 아니라 진짜 고장이다.
`traintest2.js` 는 여기 적혀 있다는 이유로 스물다섯 판 동안 넘어갔는데,
알고 보니 `posFit` 이 두 번 선언돼서 **포지션 훈련이 통째로 죽어 있었다**
(TESTS.md 「같은 이름으로 함수를 두 번 쓰면 뒤엣것이 이긴다」).
되돌리기 전에 `git stash` 로 HEAD 기준선부터 재라 — 원래 깨져 있던 것과
내가 깨뜨린 것을 가르는 데 2분이면 된다.

**구버전 테스트(현재 깨져 있음)** — v1.5.1 의 라인업 발표 게이트를 안 타서 실패한다:
`finaltest.js` `hltest.js` `fintest.js`

### 3. 함수를 자동으로 지우지 않는다

'안 쓰는 함수 정리'를 스크립트로 돌리다가 **멀쩡한 `posTrained` 를 같이 지운 적이 있다**
(앞 주석을 지우는 범위가 넘쳤다). 참조가 세 군데 남아 있어서
포지션 훈련한 선수가 있으면 선수단 화면이 통째로 죽었다.

지울 거면 하나씩 참조 수를 다시 세고 확인한다. `lintcheck.js` 가 이걸 잡는다.

---

## 이 코드베이스의 성격

- **주석은 한국어**, 반말체. "왜 이렇게 했는지"와 "예전에 뭐가 터졌는지"를 남긴다.
  `[버그 이력]` `[제보]` 로 시작하는 주석은 실제로 겪은 문제의 기록이다. 지우지 마라.
- **선수 14명은 실존 인물**이고 능력치는 2026시즌 실제 박스스코어에서 뽑았다.
  능력치를 바꿀 거면 실제 기록을 근거로 대고, 주석에 그 근거를 남긴다.
- 상대 23팀과 그 팀 투수 방어율도 실제 기록이다.
- 게임 텍스트는 **사회인야구 단톡방 말투**다. 존댓말/반말은 `say()` `sayTo()` 가
  플레이어가 고른 선수 기준으로 정한다. 직접 하드코딩하지 마라.
- 대사는 풀(`const XXX_LINES=[['반말','존댓말'], ...]`)에 넣고 `pickFresh()` 로 뽑는다.
  한 줄짜리로 박아두면 시즌에 수십 번 같은 말이 나온다.

## 건드릴 때 조심할 것

- **용량** — 지금 약 1.96MB (gzip 0.91MB). **기준은 4MB / gzip 1.5MB 다.**
  [결정 2026-08-26] "2mb넘으면 뭐어때 인게임에만 지장안가면 된다" —
  맞는 말이다. 파일 크기는 첫 다운로드에만 걸리고 인게임 성능과는 무관하다.
  진짜 비용은 업데이트할 때마다 팀원들이 그만큼 다시 받는 것이라, 실제로
  망을 타는 gzip 쪽을 같이 본다.
  사진은 360×480 JPEG 25KB 규격을 지킨다.
- **세이브 호환** — `ST` 구조를 바꾸면 `normalizeState()` 가 옛 세이브를 메워줘야 한다.
  `compattest.js` 가 구버전 세이브 로드를 검사한다.
- **CSS 클래스 이름 충돌** — 스타일시트가 길어서 같은 이름을 두 번 쓰기 쉽다.
  `lintcheck.js` 가 중복 셀렉터를 잡는다.
- **함수 이름 충돌** — 파일이 3만 줄이라 같은 이름으로 함수를 두 번 쓰기도 쉽다.
  선언은 **뒤엣것이 앞엣것을 덮는다.** `lintcheck.js` 는 CSS·id 만 보고 이건 못 잡는다.
  새 함수를 쓰기 전에 `grep -n "^function 이름" index.html` 로 세라.
  `posFit` 이 두 번 선언돼서 포지션 훈련이 스물다섯 판 동안 죽어 있었다.
- **jsdom 한계** — 테스트 환경은 CSS 특이도를 무시하고 소스 순서만 본다.
  특이도로 이기는 스타일은 테스트로 검증이 안 되니, 이름을 갈라라.
