# 테스트

전부 [jsdom](https://github.com/jsdom/jsdom) 위에서 실제 화면을 띄워 검사한다.
빌드가 없으므로 `index.html` 을 그대로 읽어 돌린다.

```bash
npm install          # jsdom 한 번만
node lintcheck.js    # 정적 점검 (선언 사라진 함수 · 중복 CSS · 중복 키 · 용량)
node verify.js index.html   # 엔진 캘리브레이션 6개 지표
node soaktest.js     # 풀 시즌을 실제 UI 경로로 완주 — 가장 중요한 통합 테스트
```

## 전부 돌리기

```bash
for f in lintcheck verify vernotetest mgrtest kakaotest careertest nametest \
         awardtest kingtest savediet loadtest sorttest phototest bgmtest \
         iostest galaxytest compattest boxtest pcardtest feattest \
         unhappytest hometest namecheck smoketest fixtest recruittest \
         traintest2 vartest wltest advtest dectest2 subtest resumetest ruletest pitchbug deptest pitchtest playtest swaptest vartest2 rottest qualtest; do
  printf "%-13s " $f
  if [ "$f" = "verify" ]; then node verify.js index.html >/dev/null 2>&1 && echo OK || echo FAIL
  else node $f.js >/dev/null 2>&1 && echo OK || echo FAIL; fi
done
node soaktest.js
```

## 무엇을 보는지

| 파일 | 검사 대상 |
|---|---|
| `lintcheck` | 선언이 사라진 채 호출되는 함수 · 중복 CSS 셀렉터 · 객체 중복 키 · 용량 |
| `verify` | 엔진 캘리브레이션 — K% · BB% · BABIP · 2루타 · 3루타 · 홈런 비율 |
| `soaktest` | 풀 시즌 완주(주 시작 → 우천 → 용병 → 경기 → 확정) · 세이브 왕복 · 새 시즌 이월 |
| `compattest` | 구버전 세이브 호환 · 인원 부족 · 0 나눗셈 · 플레이오프 |
| `smoketest` | UI 3경기 + 전 화면 훑기 |
| `boxtest` | 박스스코어 — 선발 + 대타 전원 기록 |
| `pcardtest` | 선수 카드 (타자/투수 구분) |
| `feattest` | 진기록 16종 판정 · 보관 |
| `awardtest` `kingtest` | 구간 시상 · 시즌 종합왕 · 포디움 |
| `dectest2` | 직접 지휘 — 지시없음이 판단 횟수를 안 먹는지 · 투수 교체 상시 버튼 |
| `subtest` | 투수 교체 시 타순·수비 정리 · 지명타자 소멸 (지타 유무 모두) |
| `resumetest` | 경기 중 다른 탭 갔다 와도 이어서 진행 · 재굴림 차단 |
| `swaptest` | 두 번 눌러 타순·포지션 교체 (명단/야구장) |
| `vartest2` | 불참 사유·또래 잡담 확장 · 한 시즌 대사 중복도 |
| `playtest` | 직접 플레이 — 타이밍 스윙 · 코스 선택 · 모드별 노출 빈도 · 보정 폭 |
| `qualtest` | 규정이닝(경기당 1이닝) 시상 기준 · 타석·마운드 얼굴 |
| `rottest` | 로테이션 회전 · 한 시즌 투수 분산 · 선발 한계 이닝 |
| `pitchtest` | 등판 성장·사기·불씨 해소 · 미등판 불만 누적 · 사람별 말버릇 |
| `deptest` | 야구장 그림 라인업 · 포지션별 뎁스차트 · 주포지션 1순위 중복 방지 |
| `pitchbug` | 교체 아웃된 선수의 투수 복귀 차단 · 등판 예정 투수/주전 보호 |
| `ruletest` | 교체 복귀 금지 · 결장자 차단 · 승계주자 자책 · 콜드/마지막이닝 말 스킵 · 이탈자 영구 제외 |
| `careertest` | 통산 연도별/총합 · 시즌 로그 |
| `sorttest` | 표 정렬 · 구단 통산 1위 |
| `unhappytest` | 불만 규칙 (등판 면제 · 본인 면제 · 라이벌) |
| `nametest` | 화면별 선수 이름 클릭 |
| `mgrtest` | 감독 액션 · 물통 자해 · 상황별 답변 |
| `kakaotest` | 단톡 대사 다양도 측정 |
| `vernotetest` | 튜토리얼 · 업데이트 안내 모달 |
| `bgmtest` `iostest` | 사운드 · 아이폰 해금 경로 |
| `galaxytest` | 갤럭시(삼성인터넷) 대응 |
| `loadtest` | 대기화면 |
| `phototest` | 선수 사진 |
| `hometest` | 홈/원정 · 라인스코어 정합성 |
| `namecheck` | AI 투수 이름 생성 |
| `savediet` | 세이브 감량이 무손실인지 |
| `perftest` | 화면 렌더 · 경기 시뮬 속도 (참고용) |
| `wintest` | 승리·패전투수 판정 (결승점 기준) |
| `dectest` | 직접 지휘 판단창 — 오늘 기록 표시 · 상황별 지시 |
| `scouttest` | 스카우트 투수 · 선수 카드 몸값 |
| `begtest` | 인원 부족 대응 — 사정하기 확률·순서 · 라인업 보존 |
| `itptest` | 그라운드 홈런 — 주력 조건 · 리그 비율 |
| `postest` | 라인업 수비 자리 무결성 — 투수 두 명 · 빈 자리 · 마운드 교환 |
| `hoftest` | 전시장 통산 타격·투구 · 명예의 전당(단일시즌 최고·헌액자·연표) |
| `scenetest` | 2D 그라운드 장면(장면 기술서·좌표·발동 조건) · 경기 전 리그 랭킹 |
| `slidetest` | 구종 궤적 — 도착점이 미트·판정과 일치하는지 · 좌우 맞대결별 휘는 방향 |
| `uitest` | 짧아진 튜토리얼 · 화면 첫 진입 안내 · ? 도움말 · 구장 그림·전광판 |
| `bdaytest` | 생일 이벤트 — 진짜 달력 기준 · 홈 카드 · 축하 릴레이 · 당일 버프 |
| `rottest2` | 구속(실제 스피드건 기준·랜덤) · 로테이션 터치 변경 · 라인업 동기화 · 홈런 비거리 |
| `umptest` | 심판 오심·항의 · 유인구 · 구종별 타격 · 타자 정보 · 하이라이트 상한 |
| `wiftest` | 만약에 — 순수 시뮬레이션(세이브 무영향·원본 능력치) · 출전 명단 · 투수 순서 · 지명타자 · 타순 편집 |
| `chasetest` | 유인구 네 방향이 화면을 얼리지 않는지 |
| `boardtest` | 중계 전광판 숫자 겹침 (사파리 최악 흉내) — **크로미움 필요** |
| `axistest` | 화면 축 — 투수↓공↓타자↓포수 · 타자가 타석 박스 안에 서는지 · 투구 동작 순서 — **크로미움 필요** |
| `chasetest` | 유인구 네 방향이 예외 없이 던져지는지 |
| `champtest` | 우승 세리머니 — 색종이·헹가래·다시 보기 (준우승에는 안 뜬다) |
| `scumtest` | 새로고침·다시 누르기로 결과를 다시 굴릴 수 있는지 |
| `numtest` | 유니폼 등번호 자리 · 앞 소매가 가슴 한자를 덮는지 (크로미움) |
| `swingtest` | [스윙] 버튼이 지켜보기와 분리돼 있는지 · 기존 judge() 에 붙는지 · 버튼 스윙의 자리 판정 |
| `boardtest` | 중계 전광판 숫자 겹침 (폭 5종) — **크로미움 필요** |
| `arttest` | 브라우저가 있어야 되는 검사 — 파츠 시트 알파 · 뼈 뒤집기 · 통짜 그림(타자·투수) · 베이스 좌표 · 다리 이음매 · 만약에 라인스코어 겹침 — **크로미움 필요** |

## 가끔 실패하는 것

확률 기반 단정문이 들어 있어 간헐적으로 실패한다. **3회 재실행해서 판단한다.**

`advtest` `traintest2` `wltest` `recruittest` `fintest` `awardtest` `mgrtest` `dectest2` `hltest` `subtest`

`dectest2` 의 '수비 판단창이 여러 번' 은 판단 발생이 확률이라 절반쯤 걸린다.
`hltest` 의 '박스스코어 대조' 도 교체가 끼면 어긋나서 자주 걸린다.
`subtest` 의 '지명타자 상태' 는 자동 교체가 확률이라 12회 중 3회쯤 걸린다.

## 구버전 테스트

`finaltest` `hltest` `fintest` 는 v1.5.1 의 '라인업 발표' 게이트를 안 타서
한동안 깨져 있었다. 지금은 고쳐서 다 돈다.
`enterGame()` 헬퍼로 경기 화면을 거쳐서 시작한다.

## 경기 시작 버튼을 누르는 테스트

v2.19.0 부터 **경기 시작 전에 리그 랭킹 화면이 먼저 뜬다.**
`직접 지휘` / `자동 진행` 을 누른 뒤에는 `passRank()` 를 불러서 넘겨야
경기가 시작된다. 해당 테스트에는 헬퍼가 이미 들어 있다.

## 캔버스를 쓰는 화면

v2.20.0 부터 구장 그림(`.mv-cv`)과 2D 장면이 캔버스다. jsdom 은 `getContext`
가 없어서 **`Not implemented: HTMLCanvasElement.prototype.getContext`** 경고를
낸다. 게임은 이 경우 그림만 건너뛰고 정상 동작한다.
테스트의 `jsdomError` 필터에 `not implemented` 를 넣어야 오탐이 안 잡힌다.

## 생일 테스트

`bdaytest` 는 실제 실행일과 무관하게 돌아야 해서 `todayMD()` 를 덮어써서
날짜를 고정한다. 새 생일을 `BIRTHDAY` 에 추가할 때는 `'MM-DD'` 형식만
지키면 되고 테스트는 손댈 필요 없다.

## 만약에 (순수 시뮬레이션)

v2.28.0 부터 `만약에` 는 지금 세이브와 **완전히 분리된 판**이다.
`whatIfRoster()` 가 `WWZW` 상수(2026 실제 기록에서 뽑은 원본 능력치)를
로스터에 덮어씌우고, 컨디션·사기·피로·부상·결장·장비·작전을 전부 뺀다.

그래서 `whatIfRun()` 은 **같은 시드면 세이브가 어떤 상태든 같은 결과**가
나와야 한다. `wiftest` 가 이걸 검사한다 — 능력치를 올려놓거나 컨디션을
10 으로 떨어뜨려도 점수가 안 바뀌어야 한다.
돌린 뒤 `ST.bat` `ST.pit` `ST.stand` `ST.cond` `ST.chat` 이 한 글자도
안 바뀌는지도 같이 본다.

라인업은 `whatIfBest(선발id)` 로 짠다. 선발 투수를 야수 자리에서 빼는 게
중요하다 — 우리 팀은 다들 포지션이 여러 개라, 안 빼면 송승민이 던지면서
포수까지 보는 라인업이 나온다.

## 파츠 시트 (사람 그림)

`arttest` 는 다른 테스트와 달리 **크로미움을 띄운다.** 캔버스로 알파를 읽어야
해서다. playwright 가 없으면 조용히 건너뛴다.

```bash
NODE_PATH=/opt/node22/lib/node_modules node arttest.js
```

### 왜 이 테스트가 있나

v2.27.0 의 시트는 **알파가 밝기로 잘못 만들어져 있었다.** 어두운 픽셀이 전부
투명해서, 모자 크라운·헬멧 돔·유니폼 소매·스파이크·배트 몸통이 통째로 뚫려
있었다. 화면에서는 그 자리에 잔디가 비쳐서 **헬멧이 초록색으로 보였다.**
눈으로는 "색이 이상하다" 로만 보여서 원인을 못 찾는다.

`arttest` 는 시트 바깥에서 투명 픽셀을 칠해 들어가서, 안 칠해진 투명 픽셀
(= 그림 안쪽 구멍)이 하나라도 있으면 잡는다. 파츠별 급소 여섯 군데도 따로 본다.

### 시트를 새로 내보낼 때

알파가 또 깨졌으면 이 방법으로 메운다 — 바깥에서 이어지는 투명 픽셀만 남기고,
안쪽 구멍은 알파 255 로 채운 뒤 webp quality 90 으로 다시 저장한다.
원본 RGB 는 투명 픽셀에도 남아 있으므로 색은 그대로 살아난다.

### 뼈를 뒤집을 때

`mvBone(g,sheet,k,len,mir)` 의 `mir` 은 **뼈 축 기준**으로 뒤집는다.
회전보다 **먼저** `scale(-1,1)` 을 걸어야 뼈 끝(무릎·팔꿈치)이 제자리에 남는다.

```js
if(mir) g.scale(-1,1);
g.rotate(Math.atan2(vx,vy));
g.scale(sc,sc);
```

[버그 이력] 예전엔 `rotate` 뒤에 `scale(-sc,sc)` 로 뒤집었다. 그러면 그림의
세로축 기준으로 뒤집혀서 뼈 끝이 `(-2·sc·vx·vy/L, sc(vy²-vx²)/L)` 로 간다.
넓적다리는 3.6칸 밀렸고, 그 자리에 정강이를 붙이니 무릎에서 다리가 꼬였다.
`arttest` 의 `[뼈 뒤집기]` 가 네 뼈 전부 `(0, len)` 에 떨어지는지 본다.

### 관절 좌표를 고칠 때

관절은 **그 높이의 불투명 픽셀 중심**에 박아야 한다. 눈대중으로 적으면
파츠끼리 반대쪽으로 밀려서 이음매가 어긋난다.
파이썬으로 파츠 박스를 가로줄마다 훑어서 중심을 재고 그 값을 쓴다.

### 옛 브라우저의 flex

`[만약에 라인스코어]` 검사는 `.wl-r i{min-width:0}` 을 **일부러 끼워 넣고** 잰다.

옛 브라우저(삼성 인터넷 구버전·옛 안드로이드 웹뷰)는 flex 항목의
`min-width:auto` 를 구현하지 않아서 **내용보다 더 줄어든다.**
그러면 이닝 칸을 담은 `<i>` 가 세 칸 폭으로 쪼그라들고, 안에 든 칸들은
그대로 밖으로 삐져나와 R·H·E 밑에 깔린다. 실제 제보 화면이 딱 이거였다.
최신 크롬에서는 `min-width:auto` 덕분에 안 나서, 그냥 열어보면 못 찾는다.

그래서 **폭이 고정된 칸을 품은 flex 상자에는 `flex:0 0 auto` 를 준다.**
`min-width:auto` 에 기대지 마라. 줄 자체는 `width:max-content; min-width:100%`
로 두고 바깥에서 `overflow-x:auto` 로 민다.

## 통짜 그림 (타자 · 투수)

v2.31.0 부터 **타자와 투수는 관절 조립을 안 한다.** 그림 한 장(`MV_FIG_SRC`)
으로 그린다. 몸이 애초에 이어져 있으니 이음매가 생길 자리가 없다.
야수 아홉 명과 포수는 그대로 파츠(`MV_SHEET_SRC`)를 쓴다 — 20px 짜리에서는
티가 안 난다.

- `MV_FIGS` 상자, `MV_FIG_ANCH` 는 `[발 중앙 x, 머리끝 y, 발바닥 y]` (상자 비율)
- `mvFig(g,k,x,y,h,u,pose,flip)` — `(x,y)` 가 **발바닥**, `h` 가 머리끝~발바닥
- `MV_FIG_POSE[pose]` = `[가로배율, 세로배율, 앞뒤밀기, 기울기, 배트각, 다리모으기]`

### 배트는 몸이 아니다

배트는 물건이라 그림에서 지우고 파츠 시트의 `bat` 을 따로 얹는다.
그래야 몸은 통짜로 두면서 스윙 때 배트만 돌릴 수 있다.
지운 자리에 생긴 구멍은 이웃 색으로 메웠다 — 안 메우면 손 언저리로 잔디가 비친다.
`arttest` 가 `[통짜 그림]` 에서 구멍 개수와 '배트가 지워졌는지'를 본다.

### 다리 모으기

그림은 스트라이드를 벌린 자세라, 세트 자세로 돌리려면 다리만 좁혀야 한다.
벨트 아래를 32조각으로 잘라 배율을 조금씩 바꿔 가며 그린다.
한 번에 확 좁히면 벨트에서 몸이 계단처럼 꺾이고, 조각이 적으면 옆선이 톱니처럼 튄다.

## 사파리 텍스트 자동 확대

사파리(특히 아이폰)는 **좁은 칸 안의 작은 글자를 제멋대로 키운다.**
전광판 글자를 8px 로 잡아놔도 아이폰에서는 부풀어서 칸을 넘쳤다.
크롬에서는 안 일어나니 열어봐도 못 찾는다.

`*{-webkit-text-size-adjust:100%}` 로 전역에서 껐다.
그리고 전광판은 `mvScore()` 가 실제 너비를 재서 글자 크기를 직접 넣는다 —
칸 하나에 두 자리가 들어가려면 글자가 칸 너비의 0.55 배여야 한다.

`boardtest` 는 `.mvb-r i u{min-width:0}` 과 `font-size:12px !important` 를
**일부러 끼워 넣어** 최악을 만든 다음, 320·360·390 (아이폰 폭)에서 숫자가
옆 칸으로 삐져나오는지 본다. `BOARD_HARSH=0` 으로 돌리면 제 크기로 검사한다.

**폭이 고정된 칸에는 `overflow:hidden` 을 준다.** 넘치더라도 겹치지 말고 잘려라.

### 던지는 팔

투수는 통짜 그림이라 그대로 두면 팔이 안 움직인다 — 매 프레임 팔 벌리고
서 있는 꼴이 된다. 그래서 **던지는 팔만 어깨에서 잘라내 따로 돌린다.**

- `MV_FIGS.parm` — 팔 조각 상자
- `MV_FIG_ARM.pit` = `[어깨x, 어깨y (투수 그림 기준), 조각 안에서의 어깨x, 어깨y]`
- `MV_FIG_POSE[pose][6]` — 팔각 (그림에 그려진 화면 153도에서 얼마나 더 돌리나)
- `MV_FIG_POSE[pose][7]` — 손에 공이 있나 (팔로스루는 0)

자를 때 주의할 것:
- **어깨 그루터기를 남긴다** (피벗 반경 20px 안쪽은 몸통에 둔다). 안 그러면
  팔이 돌아갔을 때 어깨에 구멍이 뚫린다.
- 자르고 나면 **몸에서 떨어진 작은 덩어리**(소매 외곽선 조각)가 남는다.
  연결 성분을 세서 제일 큰 것만 남기고 지운다.
- 자른 자리에 **안쪽 구멍**이 생긴다. 바깥에서 안 닿는 투명 픽셀을 찾아
  이웃 색으로 메운다. 안 메우면 그 틈으로 잔디가 비친다.
- **릴리스에서 팔이 머리를 넘어가면 안 된다.** 넘어가면 공이 모자 위에 뜬 것처럼
  보인다. 머리 왼쪽(화면 262도)에서 끝낸다.

`arttest` 의 `[통짜 그림]` 이 팔각이 자세마다 다른지, 코킹에서 팔이 실제로
78px 올라가는지, 팔로스루에서 내려오는지를 캔버스 픽셀로 잰다.

## 그라운드 홈런 배수

`simPA` 의 그라운드 홈런 확률에 **구장 배수를 그대로 곱하면 두 번 먹는다.**
배나물은 이미 3루타가 1.55배 많은데, 거기서 홈까지 도는 확률에도 1.55배를
곱하면 실제로는 2.4배가 된다. 제곱근만 먹인다.

```js
const chance=clamp(LG.ITP_BASE*(0.5+fast)*sloppy*Math.sqrt(park.d3||1), 0, 0.34);
```

기준점은 **실제 2026시즌 기록**이다 — 팀 홈런은 그라운드 홈런 한 개뿐이었고
담장을 넘어간 타구는 시즌 내내 0개였다. 그 정도로 드물어야 맞다.
`verify` 의 홈런/안타% 와 `itptest` 의 그라운드 비율을 같이 보면서 맞춘다.

## 화면 축 (투수 ↓ 공 ↓ 타자 ↓ 포수)

중계 화면은 **포수 뒤에서 마운드를 보는 시점**이다. 그래서:

- 투수는 화면 **위**(마운드), 타자는 화면 **아래**(타석), 포수는 그보다 더 아래
- 공은 위 → 아래로 간다 (`throwBall` 의 릴리스 지점이 `H*0.49`)
- **우타는 3루쪽 = 화면 왼쪽**, 좌타는 1루쪽 = 화면 오른쪽

타석 박스 좌표는 `MV_BOX` 하나뿐이다. `mvField()` 가 그걸로 그리고
`mvPaint()` 가 `MV_BOXC` 로 타자를 세운다. **좌표를 두 군데 박지 마라** —
그래서 타자가 박스 밖에 서 있는 걸 오래 못 잡았다.
`axistest` 가 이 축과 박스를 전부 좌표로 검사한다.

## 완성 포즈 그림 (v2.35.0~)

타자·투수는 **조립하지 않는다.** 그림 한 장이 곧 한 자세다.

```js
const MV_FIGS={ pit:[...], bat:[...], pitw:[...] };   // 상자
const MV_FIG_OF={ idle:'pitw', wind:'pitw', cock:'pitw',
                  rel:'pit', follow:'pit', stand:'bat', swing:'bat' };
const MV_FIG_H={ pit:52, pitw:50, bat:92 };           // 화면에서의 키
mvFig(g, pose, x, y, u, flip)                          // (x,y) 가 디딘 발
```

`mvFig` 안에는 `drawImage` 가 한 번뿐이다. 머리를 얹거나 팔을 돌리거나
다리를 눌러 좁히던 코드는 전부 걷어냈다 — **자세를 코드로 만들어내려 하면
아무리 맞춰도 사람으로 안 보인다.** 자세가 더 필요하면 그림을 한 장 더 그린다.

`axistest` 의 `[완성된 그림을 그대로 쓴다]` 가 조립 코드가 되살아나면 잡고,
`[누가 누구를 보고 있나]` 가 살색 픽셀로 방향을 잰다 —
투수는 얼굴이 크게(정면), 타자는 작게(뒤통수) 보여야 한다.

## 스윙 · 등번호 (v2.36.0~)

**타격 판정은 원래부터 제대로 있었다.** `renderSwing` 의 `judge()` 가
타이밍(`tap.t-0.90`)과 자리(`|tap - 공|`)를 둘 다 재서
`q = timing^0.85 * loc^0.70` 을 만들고, q<0.18 헛스윙 / q<0.38 파울 / 그 위 컨택이다.
문제는 **눈에 안 보이는 것**이었다 — 스윙 포즈가 준비 자세와 같은 그림이라
버튼을 눌러도 화면이 그대로였다.

`mvSwingPose(t)` 가 0~1 진행도로 [몸 회전, 앞으로 밀기, 배트 잔상]을 준다.
`mvSwing()` 이 0.44초 동안 매 프레임 다시 그린다.
`axistest` 의 `[스윙이 눈에 보인다]` 가 프레임마다 그림의 무게중심이
실제로 옮겨졌다가 돌아오는지 픽셀로 잰다.

등번호는 **그림에 박아두지 않는다.** 타자 그림에서 숫자를 지우고
`MV_BAT_NO` 자리에 `jerseyNo(pid)` 를 캔버스로 찍는다.
번호를 바꿨을 때 그림이 달라지는지도 테스트가 확인한다.

## 타석 입력 (v2.37.0~)

타석에는 **행동이 셋**이다. 셋 다 서로 다른 함수로 간다.

| 버튼 | 하는 일 |
|---|---|
| `[지켜본다]` `.pl-hit` | `judge(null)` — 안 휘두른다. 심판 판정 |
| `[스윙]` `.pl-swing` | `swingAt(null)` → `mvSwing()` + `judge({t, x,y})` |
| `[알아서 치게 둔다]` `.pl-skip` | 자동 진행 |
| 야구장 탭 (보조) | `swingAt({x,y})` — 자리까지 노리는 정밀 입력 |

**버튼 스윙의 자리**는 존 한가운데에서 공 쪽으로 55%만 따라간다.
단, 공을 존 안(0~2)으로 clamp 해서 계산한다 — **존 밖으로 빠진 공은
몸이 못 따라간다.** 그래서 한가운데 정타 / 구석 빗맞음 / 존 밖 헛스윙이
자연스럽게 나온다. 판정식(`q = timing^0.85 * loc^0.70`) 은 안 건드렸다.

`swingtest` 가 두 버튼이 색·크기·문구로 구분되는지까지 본다 —
**제일 큰 버튼이 스윙이어야 한다.**

## 구종 배분 (v2.37.1~)

던질 구종은 `pickPitch(pit, rng)` 로 뽑는다. **arsenal 에서 균등 추첨하면 안 된다.**

`arsenalOf()` 는 던질 수 **있는** 구종을 주고, `pitchMix()` 가 얼마나 던지는지를 준다.
직구가 기본 58%(제구가 나쁘면 ↑, 구위가 좋으면 ↑), 남은 몫을 변화구가 나눠 갖는다.

[버그 이력] 균등 추첨이라 5종 던지는 투수는 「변화구」 하나만 노려도 80% 가 맞았다.
노림수는 둘 중 하나를 고르는 건데 한쪽이 80% 면 게임이 안 된다.
`swingtest` 의 `[구종 배분]` 이 **어떤 투수든 변화구 총합이 50% 미만**인지,
직구가 항상 최다인지, 실제로 2만 번 뽑아도 배분대로 나오는지 검사한다.

## 수비수 글러브 손 (v2.38.0~)

중계 화면은 **포수 뒤에서 보는 시점**이라 야수는 카메라를 마주 본다.
마주 본 사람의 **왼손은 화면 오른쪽**이다. 그래서

| 던지는 손 | 글러브 | 화면에서 |
|---|---|---|
| 우투 (기본) | 왼손 | **오른쪽** — `MV_POSES.idle` (`handF:'glove'`) |
| 좌투 | 오른손 | 왼쪽 — `MV_POSES.idleL` (`handB:'glove'`) |

[버그 이력] 기본 포즈가 `handB:'glove'` 라 아홉 명 전원이 좌투로 그려졌다.
**좌우 반전으로 때우면 안 된다** — 얼굴·몸통·다리까지 뒤집혀서 경기장 방향과
어긋난다. 글러브를 어느 팔에 물리느냐만 바꾼다.

자리별 선수는 `MV_FIELD_SPOT`(3B·SS·2B·1B) 과 `fielderThrowsL(pos)` 로 찾는다.
경기 중이 아니거나 못 찾으면 우투로 본다.
`axistest` 의 `[수비수 글러브]` 가 실제로 그려서 **갈색 픽셀 무게중심이
어느 쪽인지**까지 잰다.

## 유니폼 등번호와 어깨 이음매 (v2.39.0~)

### 소매는 몸통보다 **먼저** 그린다

[제보] "양쪽 팔이 몸통과 자연스럽게 연결되지 않고 어깨에서 기괴하게 꺾여 있다"

각도도 어깨(`MV_SH`)도 회전 기준점도 멀쩡했다. **그리는 순서**가 원인이었다.
`sleeve` 파츠는 어깨를 통째로 덮는 둥근 덮개라 폭이 5.2칸인데
몸통 흰 부분은 8.5칸뿐이다. 앞쪽 팔을 몸통 **위에** 통째로 얹으니
소매가 가슴을 1.9칸(그림 20px) 덮어서 한자 「腕」 이 잘리고
어깨선이 가슴 한복판에서 꺾인 것처럼 보였다.

사람을 정면에서 보면 몸통이 어깨 안쪽을 가린다. 그래서 `mvArm(...,layer)` 로
갈랐다 — **소매는 언제나 몸통보다 먼저, 팔뚝과 손만 몸통 앞에.**

```
legB legF  →  armB(통째)  →  armF 소매만('up')  →  몸통  →  등번호  →  머리  →  armF 팔뚝만('lo')
```

### 등번호는 유니폼 그림 좌표에 박는다

[제보] "등번호가 캐릭터의 왼쪽에 렌더링된다 / [한자] [번호] 순서로 읽혀야 한다"

예전엔 몸통 밖에서 `fillText` 로 **캐릭터 좌표**에 그냥 찍었다. 그래서

1. 옷을 안 따라다녔다 (몸이 기울어도 번호는 안 기울었다)
2. 좌우 뒤집기가 두 번 걸려서 **좌투는 화면 왼쪽, 우투는 오른쪽** — 선수마다 자리가 바뀌었다
3. 한자보다 위·왼쪽이라 소매에 반쯤 걸쳐 [번호][한자] 로 읽혔다

이제 `mvBoneTx(g,'torso',...)` 로 **유니폼 그림의 로컬 좌표계**를 걸고 찍는다.
자리는 `MV_FIELDER_NO`(그림 안 비율)에 있다 — 단추선(가로 0.52)보다 오른쪽,
한자 아래단(세로 0.59)보다 아래. 색은 `mvInkFor(u)` 가 유니폼 색상각을 따라간다.

타자·투수는 통짜 그림이라 별도다 — `MV_BAT_NO`(그림 픽셀). 등을 보이고 서 있어서
자리가 다르다. 이름을 갈라 둔 이유가 이것이다.

### 무엇으로 잡나

`numtest` (크로미움 필요). 실제로 그려서 픽셀을 센다.

| 검사 | 어떻게 |
|---|---|
| 앞 소매가 한자를 안 덮는가 | 팔까지 그린 것과 몸통만 그린 것의 한자 띠 잉크 픽셀 수를 **양쪽으로** 비교. 소매 천도 같은 네이비라 가리면 픽셀이 오히려 늘어난다 |
| 번호가 한자 오른쪽 아래인가 | 잉크 무게중심이 몸통 중앙보다 오른쪽, 한자 아래단보다 아래 |
| 좌투·우투 자리가 같은가 | `flip` 을 켜고 끄고 두 번 그려서 무게중심 차이 4px 미만 |

옛 코드로 돌리면 세 개 다 실패한다.

## 다시 굴리기 차단 (v2.40.0~)

[제보] "새로고침 하면 못 온다고 하는 사람 바뀜.
새로고침을 이용해서 이길 때까지 돌리는 게 가능해요"

원칙은 하나다. **결과를 정하는 난수는 저장된 상태에서만 나온다.**
`Math.random()` 이나 `Date.now()` 로 굴린 것은 새로고침하면 다시 뽑히고,
다시 뽑힐 수 있으면 마음에 들 때까지 굴릴 수 있다.

| 어디 | 예전 | 지금 |
|---|---|---|
| 번복 불참 (`buildKakaoPre`) | `Math.random` | `ST.seed + round + weekSeq` 로 고정 |
| 경기 (`makeLive`) | `ST.seed + round + Date.now()%100000` | `ST.seed + round + weekSeq` |
| 라인업 화면 '카톡 발표' | 잠금 없음 — 계속 누를 수 있었다 | `ST.announced` 면 '카톡방 열기' |
| 경기 화면 '사정해본다' | 잠금 없는 옛 판 (확률도 45% 로 달랐다) | `shortHandedButtons` 한 벌로 통일 (`begAsked`, 20%) |

우취로 한 주를 다시 굴리면 `weekSeq` 가 올라가니 시드도 같이 바뀐다.
그건 게임이 의도한 재추첨이라 그대로 둔다.

직접 지휘에서 내 선택이 달라지면 결과도 달라진다 — 그건 실력이라 막지 않는다.
막는 건 **같은 선택으로 다른 결과를 얻는 것**뿐이다.

### 시뮬레이션 테스트를 쓸 때 주의

경기 난수가 `ST` 에서 나오므로 `makeLive()` 를 반복 호출해도 **같은 경기**가 나온다.
확률을 재는 테스트(`mgrtest` 의 물통차기 3%, `playtest` 의 판단 횟수 비교)는
루프 안에서 `ST.seed` 를 돌려야 한다. 안 돌리면 4000경기가 한 경기다.

### 무엇으로 잡나

`scumtest`. 같은 주 상태를 되돌려 놓고 두 번·세 번 굴려서 결과가 같은지 본다
(새로고침한 것과 같은 조건이다). 옛 코드로 돌리면 네 개가 실패한다.

아무도 안 빠지는 주면 "늘 같다" 가 우연히 통과하므로, 실제로 빠지는 사람이
나오는 주를 먼저 찾아서 거기서 검사한다.

## 명예의 전당 (v2.41.0~)

[제보] "명예의 전당 헌액자도 조건 더더더 까다롭게 해야할듯. 경기만 나오면 누구나 하겠네.
그리고 구단별 단일 시즌 최고 기록도 규정타석 규정이닝 맞게 되는 거지?"

### 헌액

**자격**(통산 120경기 또는 150이닝)을 넘긴 사람만 심사하고,
그중 **업적 하나**를 해내야 들어간다. 출전 수는 자격일 뿐 업적이 아니다.

숫자는 눈대중이 아니라 **세 시즌을 실제 경기 흐름으로 돌려서** 잡았다
(결장·부상·로테이션 포함). 한 시즌에 이 정도가 쌓인다:

| | 한 시즌 |
|---|---|
| 주전 야수 | 22경기 · 안타 40 · 타점 25 · 도루 33 · 홈런 1~2 |
| 에이스 | 15등판 · 39이닝 · 39탈삼진 · 4승 · 개인 타이틀 6개 |

여기에 맞춰 **여덟 시즌쯤 최고로 뛰어야 하나 걸리는** 선으로 올렸다.
홈런만 반대로 20 → 12 로 낮췄다 — 20개는 열다섯 시즌짜리라 있으나 마나였다.

'구단 단일 시즌 최고 기록 보유' 도 헌액 사유로 넣어봤다가 뺐다.
1년차가 끝나면 그 열두 부문을 잘하는 서너 명이 통째로 나눠 갖는다.
한 시즌만 뛰고도 절반을 들고 있으니 까다롭게 하려는 것과 정반대다.

### 규정타석 · 규정이닝

비율 기록(타율·OPS·방어율)은 **원래부터** `qualPA`/`qualOuts` 를 걸고 있었다.
누적 기록(안타·홈런·이닝…)에 최소 기준이 없는 것도 맞다.

고친 건 **진행 중인 시즌의 기준 경기 수**다. 예전엔 그때까지 치른 경기로
기준을 잡아서 개막 3경기에 6타석 4안타면 타율 .667 이 구단 기록으로 올라왔다.
이제 `gMark(y)` 가 진행 중인 시즌은 **다 치렀을 때**를 기준으로 잡는다.
시즌이 끝나야 구단 기록에 낄 수 있다는 뜻이고, 그게 규정타석의 원래 뜻이다.

그 해 팀 리더(`gOf`)는 지금까지 치른 만큼으로 그대로 둔다 — 시즌 내내
'타율 1위 없음' 이면 화면이 죽는다.

### 무엇으로 잡나

`hoftest`. 출전만 많은 사람(400경기 120안타)이 안 들어가는지,
업적을 넘긴 사람은 들어가는지, 근속 미달이 걸러지는지를 실제 화면의
헌액자 줄로 확인한다.

## 우승 세리머니 (v2.42.0~)

[요청] "우승하면 뭔가 축하하는 그런 모션 나오면 좋을듯"

한 시즌을 다 돌려야 한 번 볼까 말까 한 장면이라 여기만큼은 요란해도 된다.
색종이 44장이 쏟아지고, 트로피가 튀어나와 둥실거리고,
그날 결승에 나온 사람들이 헹가래를 친다. 내가 고른 선수만 더 높이 뛴다.

**전부 CSS 애니메이션이다** — 캔버스도 타이머도 안 쓴다. 느린 기기에서
버벅이지 않고, 화면을 닫으면 그걸로 끝이다. 움직임 줄이기
(`prefers-reduced-motion`)는 파일 맨 위 전역 규칙이 이미 animation 을 끈다 —
여기서 또 쓰면 `lintcheck` 의 중복 셀렉터에 걸린다.

`@keyframes` 의 `0%`/`100%` 도 줄 맨 앞에 오면 셀렉터로 세어진다.
새 keyframes 는 **한 줄로** 쓰거나 들여쓰기 없이 이어 붙여라.

뜨는 곳은 두 군데뿐이다.
- `commitGame` — 결승을 이긴 그 순간 (`ST.playoff.place==='우승'` 일 때만)
- 시즌 종료 화면의 「우승 세리머니 다시 보기」 — 그때 화면을 못 본 사람용

`champtest` 가 색종이가 제각각 떨어지는지(자리·시차가 다 다른지),
내 선수 표시가 하나뿐인지, 준우승에는 안 뜨는지까지 본다.

