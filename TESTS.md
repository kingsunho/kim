# 테스트

전부 [jsdom](https://github.com/jsdom/jsdom) 위에서 실제 화면을 띄워 검사한다.
빌드가 없으므로 `index.html` 을 그대로 읽어 돌린다.

```bash
npm install          # jsdom 한 번만
node lintcheck.js    # 정적 점검 (선언 사라진 함수 · 중복 CSS · 중복 키 · 용량)
node verify.js index.html   # 엔진 캘리브레이션 6개 지표
node soaktest.js     # 풀 시즌을 실제 UI 경로로 완주 — 가장 중요한 통합 테스트
```

## 전부 돌리기

```bash
for f in lintcheck verify vernotetest mgrtest kakaotest careertest nametest \
         awardtest kingtest savediet loadtest sorttest phototest bgmtest \
         iostest galaxytest compattest boxtest pcardtest feattest \
         unhappytest hometest namecheck smoketest fixtest recruittest \
         traintest2 vartest wltest advtest dectest2 subtest resumetest ruletest pitchbug deptest pitchtest playtest swaptest vartest2 rottest qualtest; do
  printf "%-13s " $f
  if [ "$f" = "verify" ]; then node verify.js index.html >/dev/null 2>&1 && echo OK || echo FAIL
  else node $f.js >/dev/null 2>&1 && echo OK || echo FAIL; fi
done
node soaktest.js
```

## 무엇을 보는지

| 파일 | 검사 대상 |
|---|---|
| `lintcheck` | 선언이 사라진 채 호출되는 함수 · 중복 CSS 셀렉터 · 객체 중복 키 · 용량 |
| `verify` | 엔진 캘리브레이션 — K% · BB% · BABIP · 2루타 · 3루타 · 홈런 비율 |
| `soaktest` | 풀 시즌 완주(주 시작 → 우천 → 용병 → 경기 → 확정) · 세이브 왕복 · 새 시즌 이월 |
| `compattest` | 구버전 세이브 호환 · 인원 부족 · 0 나눗셈 · 플레이오프 |
| `smoketest` | UI 3경기 + 전 화면 훑기 |
| `boxtest` | 박스스코어 — 선발 + 대타 전원 기록 |
| `pcardtest` | 선수 카드 (타자/투수 구분) |
| `feattest` | 진기록 16종 판정 · 보관 |
| `awardtest` `kingtest` | 구간 시상 · 시즌 종합왕 · 포디움 |
| `dectest2` | 직접 지휘 — 지시없음이 판단 횟수를 안 먹는지 · 투수 교체 상시 버튼 |
| `subtest` | 투수 교체 시 타순·수비 정리 · 지명타자 소멸 (지타 유무 모두) |
| `resumetest` | 경기 중 다른 탭 갔다 와도 이어서 진행 · 재굴림 차단 |
| `swaptest` | 두 번 눌러 타순·포지션 교체 (명단/야구장) |
| `vartest2` | 불참 사유·또래 잡담 확장 · 한 시즌 대사 중복도 |
| `playtest` | 직접 플레이 — 타이밍 스윙 · 코스 선택 · 모드별 노출 빈도 · 보정 폭 |
| `farmtest` | [2.76~2.81] 2군 콜업·강등 · 리그 이적 · 콜업/이적이 새로고침을 넘어가는지 · 졸업 후 2군 시작 · **2군 경기가 끝까지 도는지** · 훈련 코인 |
| `injtest` | [2.73~2.75] 몸(bodyLoad) 편중 → 부상 · 부상이 능력치에 남기는 자국 · 교체 사유 · 훈련한 능력치가 새로고침을 넘어가는지 · 주루 센스(sIQ) · 감독 면담 약속 |
| `runtest` | [2.72~2.80] 위 + 고교 3년이 판마다 달라지는지 · 홈런 곡선이 선수 모드에만 열리는지 · 투수 겸업 |
| `qualtest` | 규정이닝(경기당 1이닝) 시상 기준 · 타석·마운드 얼굴 |
| `rottest` | 로테이션 회전 · 한 시즌 투수 분산 · 선발 한계 이닝 |
| `pitchtest` | 등판 성장·사기·불씨 해소 · 미등판 불만 누적 · 사람별 말버릇 |
| `deptest` | 야구장 그림 라인업 · 포지션별 뎁스차트 · 주포지션 1순위 중복 방지 |
| `pitchbug` | 교체 아웃된 선수의 투수 복귀 차단 · 등판 예정 투수/주전 보호 |
| `ruletest` | 교체 복귀 금지 · 결장자 차단 · 승계주자 자책 · 콜드/마지막이닝 말 스킵 · 이탈자 영구 제외 |
| `careertest` | 통산 연도별/총합 · 시즌 로그 |
| `sorttest` | 표 정렬 · 구단 통산 1위 |
| `unhappytest` | 불만 규칙 (등판 면제 · 본인 면제 · 라이벌) |
| `nametest` | 화면별 선수 이름 클릭 |
| `mgrtest` | 감독 액션 · 물통 자해 · 상황별 답변 |
| `kakaotest` | 단톡 대사 다양도 측정 |
| `vernotetest` | 튜토리얼 · 업데이트 안내 모달 |
| `bgmtest` `iostest` | 사운드 · 아이폰 해금 경로 |
| `galaxytest` | 갤럭시(삼성인터넷) 대응 |
| `loadtest` | 대기화면 |
| `phototest` | 선수 사진 |
| `hometest` | 홈/원정 · 라인스코어 정합성 |
| `namecheck` | AI 투수 이름 생성 |
| `savediet` | 세이브 감량이 무손실인지 |
| `perftest` | 화면 렌더 · 경기 시뮬 속도 (참고용) |
| `wintest` | 승리·패전투수 판정 (결승점 기준) |
| `dectest` | 직접 지휘 판단창 — 오늘 기록 표시 · 상황별 지시 |
| `scouttest` | 스카우트 투수 · 선수 카드 몸값 |
| `begtest` | 인원 부족 대응 — 사정하기 확률·순서 · 라인업 보존 |
| `itptest` | 그라운드 홈런 — 주력 조건 · 리그 비율 |
| `postest` | 라인업 수비 자리 무결성 — 투수 두 명 · 빈 자리 · 마운드 교환 |
| `hoftest` | 전시장 통산 타격·투구 · 명예의 전당(단일시즌 최고·헌액자·연표) |
| `scenetest` | 2D 그라운드 장면(장면 기술서·좌표·발동 조건) · 경기 전 리그 랭킹 |
| `slidetest` | 구종 궤적 — 도착점이 미트·판정과 일치하는지 · 좌우 맞대결별 휘는 방향 |
| `uitest` | 짧아진 튜토리얼 · 화면 첫 진입 안내 · ? 도움말 · 구장 그림·전광판 |
| `bdaytest` | 생일 이벤트 — 진짜 달력 기준 · 홈 카드 · 축하 릴레이 · 당일 버프 |
| `rottest2` | 구속(실제 스피드건 기준·랜덤) · 로테이션 터치 변경 · 라인업 동기화 · 홈런 비거리 |
| `umptest` | 심판 오심·항의 · 유인구 · 구종별 타격 · 타자 정보 · 하이라이트 상한 |
| `wiftest` | 만약에 — 순수 시뮬레이션(세이브 무영향·원본 능력치) · 출전 명단 · 투수 순서 · 지명타자 · 타순 편집 |
| `chasetest` | 유인구 네 방향이 화면을 얼리지 않는지 |
| `boardtest` | 중계 전광판 숫자 겹침 (사파리 최악 흉내) — **크로미움 필요** |
| `axistest` | 화면 축 — 투수↓공↓타자↓포수 · 타자가 타석 박스 안에 서는지 · 투구 동작 순서 — **크로미움 필요** |
| `chasetest` | 유인구 네 방향이 예외 없이 던져지는지 |
| `champtest` | 우승 세리머니 — 색종이·헹가래·다시 보기 (준우승에는 안 뜬다) |
| `scumtest` | 새로고침·다시 누르기로 결과를 다시 굴릴 수 있는지 |
| `numtest` | 유니폼 등번호 자리 · 앞 소매가 가슴 한자를 덮는지 (크로미움) |
| `swingtest` | [스윙] 버튼이 지켜보기와 분리돼 있는지 · 기존 judge() 에 붙는지 · 버튼 스윙의 자리 판정 |
| `boardtest` | 중계 전광판 숫자 겹침 (폭 5종) — **크로미움 필요** |
| `disttest` | 구장 규격 · 타구 비거리 · 비거리에 맞춘 애니메이션 |
| `nptest` | 컨택하면 삼진·볼넷이 안 나오는지 · 투구수가 쌓이고 표시되는지 |
| `bgtest` | 화면을 내려놓으면 직접 지휘 중계 타이머가 멈추는지 (배터리) |
| `exiletest` | 내쫓은 선수가 상대 팀 로스터·라인업에 들어가는지 · 새로고침 후에도 유지되는지 |
| `wartest` | WAR 상수를 엔진에서 다시 재서 검증 · 투타 분리·합산 · 표본 표시 |
| `opptest` | 상대 23팀 감독 성향 — 실제 기록에서 뽑히는지 · 색깔대로 실제로 굴리는지 · 지시의 절반만 먹이는지(lean) · 리그 득점이 안 흔들리는지 · 스카우팅 카드가 매니저 「기록」만큼 길어지는지 · 매니저가 하루에 다섯 번 나오는지(그 화면에 바로) |
| `arttest` | 브라우저가 있어야 되는 검사 — 파츠 시트 알파 · 뼈 뒤집기 · 통짜 그림(타자·투수) · 베이스 좌표 · 다리 이음매 · 만약에 라인스코어 겹침 — **크로미움 필요** |

## 「돈다」는 도박이 아니다 (2.72.0)

이 판의 제일 큰 규칙이다. **결과가 먼저, 판단은 그 위에서.**

예전에는 「돌아? 서?」를 먼저 묻고 그 답을 넣어서 엔진을 굴렸다. 그래서
화면에 날아가는 공은 방향만 진짜였고 거리는 시간에 비례한 가짜였다.
내야 땅볼도 화면에서는 외야로 뻗어 나갔다 —
「분명 공은 저 멀리 가는데 돈다 누르면 갑자기 땅볼아웃」이 그래서 나왔다.

지금은 순서가 반대다.

1. 배트에 맞으면 `LIVE._stretch={ask:true}` 만 예약하고 바로 엔진을 굴린다
2. `simPA` → `stretchRun` 이 `ask` 를 보고 **늘리기 판정을 안 한다.**
   안타 종류만 정해서 `res.stretchAsk` 로 넘긴다
3. `pa()` 가 안타를 그대로 적용하고 `_stretchOffer` 를 남긴다
   (갈 베이스가 비어 있을 때만 — 2루에 주자가 있으면 애초에 갈 데가 없다)
4. 화면(`showRun`)이 **진짜 비거리**로 타구를 날리고, 그 위에서 묻는다
5. 답을 `LIVE.resolveStretch(go, risk)` 가 받아서 `stretchOdds` 로 굴린다

확률식이 `stretchOdds` 한 군데에 있는 게 중요하다. 엔진 쪽(자동 진행)과
화면 쪽(직접 플레이)이 따로 쓰면 언젠가 어긋난다. `runtest` 가 그걸 본다.

**늘리려다 죽어도 안타는 남는다.** 야구 기록 규칙 그대로다 —
1루타를 치고 2루에서 죽으면 「안타 하나 + 주루사」지 안타가 사라지지 않는다.
예전엔 `{type:'OUT'}` 을 돌려줘서 타율이 부당하게 깎였다.

## 수비는 손가락으로 조종한다 (2.72.0)

「안전하게 / 달려든다」 버튼을 없앴다. `defChaseScene` 이 캔버스에
그라운드를 그리고, 손가락이 가리키는 쪽으로 내 야수가 뛴다.
공에 제일 가까이 붙었던 거리가 그대로 `q`(0~1) 가 되고,
`consumePlayMods` 가 그걸 babip·err 배수로 바꾼다. 타석의 스윙과 같은 방식이다
— 결과를 정하지 않고 확률만 민다.

여기서 **비거리를 미리 아는 방법**이 하나 더 필요했다. `battedBall` 은
`bbRng(seq)` 로 세 번 뽑아 각도를 만들고 **네 번째**를 비거리에 쓴다.
그 네 번째가 `battedU(seq)` 다 — 결과(kind)를 몰라도 이미 정해져 있다.
그래서 화면에 그린 타구가 나중에 로그에 찍히는 것과 안 어긋난다.

`leadScene` 도 같은 식이다. 1루에서 옆으로 끌면 리드가 벌어지고,
리드가 11px 을 넘으면 견제가 온다. `LiveGame.prototype.runPickoff(back)`
이 그 판정을 한다.

## 2막은 1막을 이어받는다 (2.72.1)

`groundScene.replay(res, ms, onDone, opt)` 의 `opt.at` 이 핵심이다.

처음 만들었을 때 2막은 **언제나 홈플레이트에서** 공을 다시 날렸다.
그런데 1막에서 공은 이미 외야까지 가 있다. 그래서 「돈다」를 누르는
순간 공이 홈으로 되감기고 순식간에 잡히는 것처럼 보였다 —
「돈다 버튼이 뭔가 초기화같음」이 그 말이었다.

`opt.at` 이 1막의 진행도를 넘긴다.

| 값 | 뜻 | 1막에서 쓰는 식 |
|---|---|---|
| `at.ball` | 공이 낙구점까지 간 정도 | `k/0.66` |
| `at.chase` | 야수가 공에 붙은 정도 | `(k-0.28)/0.62` |
| `at.run` | 내가 1루까지 간 정도 | `k*1.15` |

**1막의 식을 고치면 `showRun` 의 `stageAt()` 도 같이 고쳐야 한다.**
두 군데가 어긋나면 그 순간 다시 튀는 것처럼 보인다.

`K_CATCH` 도 `(1-bFrom)` 을 곱한다 — 이미 다 날아간 공은 바로 잡는다.

## 로스터는 세이브에 없다 — 세 번째 사고 (2.73.0)

메모리에도 적혀 있는 그 함정이다. **로스터(`TBYID['wwzw'].players`)는
세이브에 안 들어간다.** 매번 `index.html` 상수에서 새로 만들어진다.

여기서 세 번 사고가 났다.

| 언제 | 무엇이 사라졌나 | 어떻게 고쳤나 |
|---|---|---|
| 2.71.0 | 영입해 온 선수 | `ST.signed` 명단 + `addSignedPlayers` |
| 2.71.0 | 전역 복귀 송정민 | `ST.discharged` + `addDischarged` |
| **2.73.0** | **선수 모드 훈련으로 올린 능력치** | `ST.myRatings` + `applyMyRatings` |

`applyMyTrain` 은 로스터 객체에 `p.pow += 1.0` 을 하고 끝냈다. 세이브에
흔적이 없으니 새로고침 한 번에 원래 값으로 돌아왔다. 여덟 주를 훈련해서
파워 55 → 61 을 만들어도 탭을 닫으면 55 였다.

**로스터를 건드리는 코드를 쓸 때는 반드시 물어라 — 이게 세이브 어디에
남는가?** 안 남으면 다음 로드에 없던 일이 된다.

얹는 순서도 중요하다: `applyHsStart()`(졸업 능력치) → `applyMyRatings()`
(그 뒤로 훈련·부상으로 바뀐 값). 반대로 하면 훈련한 게 졸업 값에 덮인다.

## 새 팀을 만들 때 능력치를 빠뜨리지 마라 (2.81.0)

2군 경기를 붙이자마자 **1회에 타석이 195개** 나오면서 경기가 안 끝났다.

```
makeFarmPlayer 는 투수 능력치를 p.pitch 안에만 넣었다
  → farmFoeTeam 이 p.stf 를 읽는다 → undefined
  → simPA 가 NaN 을 물고 아웃이 안 잡힌다 → 무한 이닝
```

`hsMakePlayer` 는 **모두에게** top-level `stf/ctl/sta` 를 준다.
사회인야구는 누구나 던지기 때문이다. 새 팀 생성기를 만들 때는
그 규약을 따라라 — `finalizeTeam` 이 요구하는 필드만이 전부가 아니다.

**`farmtest` 의 「2군 경기가 실제로 끝까지 돈다」 가 이걸 잡는다.**
새 로스터 생성기를 추가하면 같은 검사를 붙여라.

## 확률만 미는 것과 결과를 정하는 것 (2.80.0)

이 저장소의 오래된 원칙이 있었다 — **「결과를 정하지 않고 확률만 민다」.**
직접 플레이가 리그 캘리브레이션을 흔들면 안 되기 때문이다.

그런데 선수 모드에서 이 원칙이 정반대로 작동했다.

> "2루수 기준 중앙으로 온다 눌렀는데 눌러서 잡았는데 갑자기 뭐 2루타?"
> "약간 결과는 정해져있고 주루플레이로 내가 할 수 있는게 없는 것 같은 기분"

**내가 분명히 잡았는데 2루타가 나오면 조작이 장식이 된다.**
그래서 모드로 갈랐다:

| | 감독 모드 | 선수 모드 |
|---|---|---|
| 내 조작 | 확률만 민다 | **결과를 정한다** |
| 홈런 곡선 | 실측 그대로 | 파워 찍은 만큼 열린다 |

`mods.defForce`(`'out'`/`'hit'`)가 그 스위치다. `dp.q>=0.84` 면 아웃,
`<=0.40` 이면 안타. 그 사이만 확률이다. 경기당 네댓 번뿐이라
리그 지표는 안 흔들린다 — `verify` 로 확인한다.

**화면이 약속한 건 지켜야 한다.** 수비 장면이 「타구가 온다」고 했으면
그 타석은 삼진·볼넷으로 끝나면 안 된다(`mods.inPlay=true`).

## 손가락이 화면을 가린다 (2.80.0)

`2.79.0` 에서 「누른 곳으로 간다」를 가상 조이스틱으로 바꿨는데도 물렸다.

> "터치로 따라가라하니깐 핸드폰 기준으론 내 캐릭터가 가려져서 안보여"

조이스틱을 **필드 위**에 그린 게 문제였다. 손가락이 정확히 내 야수를
덮는다. **조작 요소는 그라운드 밖에 둔다** — 조이스틱도, 송구할
베이스 버튼도. 그라운드는 눈으로만 본다.

내 야수 위에는 화살표를 달아서 어디 있는지 항상 보이게 했다.

## 폰은 마우스가 없다 (2.79.0)

수비 조작을 「누른 곳으로 간다」로 만들었다가 물렸다.

> "터치로 안움직여서 그냥 아무곳 눌렀더니 외야 밖으로 순간이동해서 못잡았는데"

**손가락이 화면을 가린다.** 목표 지점을 찍는 조작은 마우스에서만 성립한다.
폰에서는 **가상 조이스틱**이 맞다 — 손가락을 댄 자리가 스틱 중심이고,
미는 방향으로 움직인다. 마인크래프트 모바일이 그렇게 한다.

```js
pointerdown → stick={ox,oy,dx:0,dy:0}   // 어디를 눌러도 그 자리가 중심
pointermove → dx,dy 를 STICK_R 로 클램프
매 프레임   → fx += dx/dd * speed * (dd/STICK_R) * dt
```

**화면을 두 개로 쪼개지 마라.** 쫓아가는 화면과 송구 화면을 따로 띄웠더니
"두 개 하기가 번거롭고 주자가 안보임" 이 나왔다. 한 캔버스에서
phase 만 바꾼다: `chase` → `throw`(베이스 탭) → `throw2`(송구) → 결과 재생.

## 판정과 화면이 어긋나면 그게 제일 나쁘다 (2.79.0)

> "더돈다 눌렀는데 공이 3루 먼저 도착해서 (…) 공이 먼저 왔어도 세잎이네"

엔진은 맞았다. 화면이 거짓말을 했다. **결과가 정해진 뒤에 그리는 구간은
그 결과에 맞춰 시간을 잡아야 한다.**

```js
const endT = decided.ok ? (arrive-150)   // 세이프면 송구보다 먼저 닿는다
                        : (arrive+360);  // 아웃이면 송구가 먼저 닿는다
```

같은 이유로 **누르면 아무 일도 안 나는 버튼을 남겨두지 마라.**
송구가 손을 떠난 뒤 「돌아간다」는 이미 의미가 없는데 버튼이 살아 있었다.
그건 고장으로 읽힌다 — 잠그고 왜 잠겼는지 적어라(「이미 던졌다」).

## 실존 인물은 로스터에서 빼지 않는다 (2.77.0)

2군 강등을 실존 선수한테도 열면서 갈림길이 하나 있었다.

| 방법 | 문제 |
|---|---|
| 로스터에서 뺐다가 다시 넣는다 | 상수에서 매번 새로 만들어지는 사람이라 **매 로드마다 다시 빼야** 한다. 이 저장소에서 네 번 사고 난 길이다 |
| **출전만 막는다** ← 고른 것 | `ST.farmReal[pid]` 가 있으면 `isAvailable()` 이 false. 부상과 똑같다. 로스터를 안 건드리니 사고가 날 자리가 없다 |

가상 선수(`ST.farm`)는 원래 상수에 없으니 로스터에 넣고 빼는 수밖에 없다.
**두 방식이 공존하는 이유가 이것이다** — 헷갈리지 마라.

이적은 다르다. 나가는 건 **원래 있던 길**(`deleteLeave` → `ST.leftPlayers`
→ `placeExile`)을 그대로 탄다. 상대 팀 로스터에 꽂히는 것까지 검증된 경로다.

## 감독 모드와 선수 모드는 다른 게임이다 (2.77.0)

2군·이적 시장은 **선수 모드에서만** 돈다(`runWeek` 안에서 `isPlayerMode()` 로 감싼다).
화면(`farmCard` `tradeCard`)도 각자 앞에서 한 번 더 막는다 —
주간 훅만 막으면 옛 세이브에 남은 데이터가 감독 모드 화면에 뜬다.

**이미 벌어진 것은 모드를 바꿔도 되돌리지 않는다.** `applyFarm`/`applyTrades` 는
모드와 상관없이 돈다 — 안 그러면 기록과 로스터가 어긋난다.

## 로스터를 건드리는 곳은 이제 다섯 군데다 (2.76.0)

`applyFarm()`(2군 콜업)과 `applyTrades()`(리그 이적)가 늘었다.
전부 `normalizeState()` 안에서 이 순서로 돈다:

```
applyLeftPlayers()   나간 사람은 계속 나가 있어야 한다
addDischarged()      전역 복귀
addSignedPlayers()   영입
applyFarm()          2군에서 올라온 사람
applyTrades()        리그 안에서 팀을 옮긴 사람
```

**전부 여러 번 불려도 한 번만 들어가야 한다(멱등).** 매 부팅마다 도는
경로이기 때문이다. `farmtest` 가 각각 두 번 불러서 확인한다.

2군 선수는 상수 어디에도 없다 — **능력치까지 통째로 `ST.farm` 에 산다.**
`META` 는 세이브에 안 들어가므로 `farmMeta(p)` 로 매번 다시 만드는데,
**아이디에서 결정적으로** 만들어야 한다(영입 선수가 던지는 손 때문에
같은 함정에 빠졌던 자리다).

## 라인업은 항상 아홉이어야 한다 (2.76.0)

2군 강등을 넣자마자 6차전쯤에 경기가 통째로 멈췄다. 원인:

```
farmDown(pid) → ST.lineup 에서 그 사람만 빼고 끝
              → 여덟 명짜리 라인업
              → stepPA 의 off.slots[off.order%9] 가 undefined
              → slot.id 에서 죽는다
```

**로스터에서 사람을 빼는 코드는 반드시 라인업을 다시 채워야 한다.**
`deleteLeave` 는 원래 그렇게 하고 있었는데(`length<9 면 recommendLineup`)
새로 만든 `farmDown` 이 그걸 빠뜨렸다.

세 겹으로 막았다:
1. `farmDown` 이 직접 채운다 (`gameRotation` 도 같이 정리)
2. `makeLive` 가 경기 들어가기 전에 아홉인지 본다
3. `stepPA` 가 빈 타순을 만나도 죽지 않고 반이닝을 넘긴다

`soaktest` 가 이걸 잡았다 — **풀 시즌을 UI 경로로 완주하는 테스트가
없었으면 못 잡았을 버그다.** 단위 테스트는 전부 통과했었다.

## 화면이 결과를 만들면 안 된다 — 그 반대도 (2.76.0)

「뜬볼 아웃이 없네」 가 그 예다. 엔진은 아웃의 43% 를 뜬공으로 내고
있었는데, 화면이 뜬공을 잡고도 **1루로 송구하는 그림**을 그렸다.
결과는 맞는데 그림이 다른 말을 해서, 유저 입장에서는 그 결과가
**존재하지 않는 것**이 됐다.

로그에 뭐라고 찍히는지만 보지 말고 **화면이 그걸 어떻게 그리는지**를
같이 봐라. 둘이 다르면 유저가 믿는 건 화면 쪽이다.

## 없는 스탯을 새로 만들 때 (2.75.0)

「주루 센스(sIQ)」 를 넣으면서 부딪힌 문제. **선수 열넷의 능력치는 2026시즌
실제 박스스코어에서 뽑은 것이다.** 없는 칸을 새로 만들면 그 값은 어디서
오나? 지어내면 그건 실측이 아니다.

그래서 `stealIQ(p)` 는 **있는 걸로 짐작한다**:

```
sIQ 가 있으면 그 값, 없으면  eye×0.55 + def×0.30 + spd×0.15
```

선구가 공을 보는 눈이고 수비가 상황 판단이라는 근거가 있다.
화면에도 「(짐작)」 이라고 적어서 실측이 아님을 밝힌다.
**내 선수만** 훈련으로 실제 `sIQ` 칸이 생기고, 그때부터 짐작을 안 쓴다.

새 스탯을 넣을 때는 이 순서로 하라:
1. 실측이 없는 사람에게 쓸 **짐작 식**을 먼저 정한다 (근거를 주석에 남긴다)
2. 실제 칸(`p.sIQ`)이 생기면 짐작보다 우선하게 한다
3. **장부(`myRatings`)에 칸을 추가한다** — 안 하면 새로고침에 날아간다
4. 화면에 실측인지 짐작인지 표시한다

## 몸 — 편중되면 부러진다 (2.73.0)

```
bodyLoad(p) = (밀어내는 힘 − 받쳐주는 몸) / 34
  밀어내는 힘 = (pow×1.15 + spd×0.85) / 2
  받쳐주는 몸 = (con×0.90 + def×0.95 + arm×0.75) / 2.6
```

전부 50 이면 정확히 0 이 나오게 계수를 맞춰놨다. **계수를 건드리면 그
기준점부터 다시 맞춰라** — 0 이 아니면 "균형 잡힌 몸"이라는 말이 거짓이 된다.

부상은 `rollMyInjury(cause, mult)` 가 굴린다. `cause` 가 부상 종류를 고른다
(전력질주/스윙/송구/슬라이딩/수비). 한 경기에 한 번만 다친다.

회복 후에 남는 자국은 `applyInjuryHit` 이 새기는데, **반드시 `myRatingsSave()`
를 부른다.** 안 부르면 위 표의 네 번째 사고가 된다.

## 원하는 투수를 올린다 (2.72.1)

`userPitcherChange(pid)` 에 id 를 주면 로테이션 순서를 건너뛴다.
`availPitchers(s)` 가 후보를 주는데, **`pIdx` 뒤쪽만 본다.**
앞쪽은 이미 던지고 내려간 사람이라 야구 규칙상 마운드에 다시 못 선다
(`peekNextPitcher` 가 지키는 것과 같은 선이다 — 여기만 뚫으면 무너진다).

건너뛴 사람은 `rot` 배열 **뒤로 옮긴다.** 그 자리에 두면 `peekNextPitcher`
가 `pIdx` 뒤쪽만 훑기 때문에 영영 못 나온다.

## 아이폰에서 스윙이 씹히던 것 (2.72.0)

세 겹으로 막았다. 하나만으로는 안 됐다.

1. `plLock(true)` — 공이 오는 동안 `body.pl-lock` 으로 스크롤을 잠근다.
   `decDone` 과 `go()` 에서 무조건 푼다. 안 그러면 화면이 굳은 것처럼 보인다
2. `.pl-act` 를 `position:sticky; bottom:6px` — 스크롤해도 버튼이 안 도망간다
3. **`pointerdown` 으로 친다.** iOS 는 손가락이 몇 픽셀만 밀려도 `click` 을
   안 쏜다. `click` 도 그대로 두지만 `swingAt` 이 `phase` 를 보고 스스로 거른다

## 가끔 실패하는 것

확률 기반 단정문이 들어 있어 간헐적으로 실패한다. **3회 재실행해서 판단한다.**

`advtest` `traintest2` `wltest` `recruittest` `fintest` `awardtest` `mgrtest` `dectest2` `hltest` `subtest` `playtest`

`dectest2` 의 '수비 판단창이 여러 번' 은 판단 발생이 확률이라 절반쯤 걸린다.
`hltest` 의 '박스스코어 대조' 도 교체가 끼면 어긋나서 자주 걸린다.
`subtest` 의 '지명타자 상태' 는 자동 교체가 확률이라 12회 중 3회쯤 걸린다.

`playtest` 의 '정타 치고 볼넷·삼진 나는 일이 거의 없다 (실측)' 은
`newGame()` 이 매번 새 시드를 뽑아서 **프로세스마다 값이 크게 튄다.**
같은 프로세스 안에서는 항상 같은 값이 나온다(시드가 고정이라). 40경기로
K% 를 재는데 임계값이 14% 라, 시드에 따라 3% 도 나오고 17% 도 나온다.
**엔진이 바뀌었는지 확인하려면 이걸 보지 말고 `verify.js` 를 봐라** —
거기는 시드를 직접 깔아서 3576경기를 돌리기 때문에 엔진이 그대로면
소수점까지 똑같이 나온다.

`advtest`(2건) `traintest2`(5건) `whatiftest`(2건) 은 **지금 HEAD 에서도
그대로 실패한다.** 되돌리기 전에 기준선부터 재라 — 새로 깨진 게 아니다.

## 구버전 테스트

`finaltest` `hltest` `fintest` 는 v1.5.1 의 '라인업 발표' 게이트를 안 타서
한동안 깨져 있었다. 지금은 고쳐서 다 돈다.
`enterGame()` 헬퍼로 경기 화면을 거쳐서 시작한다.

## 경기 시작 버튼을 누르는 테스트

v2.19.0 부터 **경기 시작 전에 리그 랭킹 화면이 먼저 뜬다.**
`직접 지휘` / `자동 진행` 을 누른 뒤에는 `passRank()` 를 불러서 넘겨야
경기가 시작된다. 해당 테스트에는 헬퍼가 이미 들어 있다.

## 캔버스를 쓰는 화면

v2.20.0 부터 구장 그림(`.mv-cv`)과 2D 장면이 캔버스다. jsdom 은 `getContext`
가 없어서 **`Not implemented: HTMLCanvasElement.prototype.getContext`** 경고를
낸다. 게임은 이 경우 그림만 건너뛰고 정상 동작한다.
테스트의 `jsdomError` 필터에 `not implemented` 를 넣어야 오탐이 안 잡힌다.

## 생일 테스트

`bdaytest` 는 실제 실행일과 무관하게 돌아야 해서 `todayMD()` 를 덮어써서
날짜를 고정한다. 새 생일을 `BIRTHDAY` 에 추가할 때는 `'MM-DD'` 형식만
지키면 되고 테스트는 손댈 필요 없다.

## 만약에 (순수 시뮬레이션)

v2.28.0 부터 `만약에` 는 지금 세이브와 **완전히 분리된 판**이다.
`whatIfRoster()` 가 `WWZW` 상수(2026 실제 기록에서 뽑은 원본 능력치)를
로스터에 덮어씌우고, 컨디션·사기·피로·부상·결장·장비·작전을 전부 뺀다.

그래서 `whatIfRun()` 은 **같은 시드면 세이브가 어떤 상태든 같은 결과**가
나와야 한다. `wiftest` 가 이걸 검사한다 — 능력치를 올려놓거나 컨디션을
10 으로 떨어뜨려도 점수가 안 바뀌어야 한다.
돌린 뒤 `ST.bat` `ST.pit` `ST.stand` `ST.cond` `ST.chat` 이 한 글자도
안 바뀌는지도 같이 본다.

라인업은 `whatIfBest(선발id)` 로 짠다. 선발 투수를 야수 자리에서 빼는 게
중요하다 — 우리 팀은 다들 포지션이 여러 개라, 안 빼면 송승민이 던지면서
포수까지 보는 라인업이 나온다.

## 파츠 시트 (사람 그림)

`arttest` 는 다른 테스트와 달리 **크로미움을 띄운다.** 캔버스로 알파를 읽어야
해서다. playwright 가 없으면 조용히 건너뛴다.

```bash
NODE_PATH=/opt/node22/lib/node_modules node arttest.js
```

### 왜 이 테스트가 있나

v2.27.0 의 시트는 **알파가 밝기로 잘못 만들어져 있었다.** 어두운 픽셀이 전부
투명해서, 모자 크라운·헬멧 돔·유니폼 소매·스파이크·배트 몸통이 통째로 뚫려
있었다. 화면에서는 그 자리에 잔디가 비쳐서 **헬멧이 초록색으로 보였다.**
눈으로는 "색이 이상하다" 로만 보여서 원인을 못 찾는다.

`arttest` 는 시트 바깥에서 투명 픽셀을 칠해 들어가서, 안 칠해진 투명 픽셀
(= 그림 안쪽 구멍)이 하나라도 있으면 잡는다. 파츠별 급소 여섯 군데도 따로 본다.

### 시트를 새로 내보낼 때

알파가 또 깨졌으면 이 방법으로 메운다 — 바깥에서 이어지는 투명 픽셀만 남기고,
안쪽 구멍은 알파 255 로 채운 뒤 webp quality 90 으로 다시 저장한다.
원본 RGB 는 투명 픽셀에도 남아 있으므로 색은 그대로 살아난다.

### 뼈를 뒤집을 때

`mvBone(g,sheet,k,len,mir)` 의 `mir` 은 **뼈 축 기준**으로 뒤집는다.
회전보다 **먼저** `scale(-1,1)` 을 걸어야 뼈 끝(무릎·팔꿈치)이 제자리에 남는다.

```js
if(mir) g.scale(-1,1);
g.rotate(Math.atan2(vx,vy));
g.scale(sc,sc);
```

[버그 이력] 예전엔 `rotate` 뒤에 `scale(-sc,sc)` 로 뒤집었다. 그러면 그림의
세로축 기준으로 뒤집혀서 뼈 끝이 `(-2·sc·vx·vy/L, sc(vy²-vx²)/L)` 로 간다.
넓적다리는 3.6칸 밀렸고, 그 자리에 정강이를 붙이니 무릎에서 다리가 꼬였다.
`arttest` 의 `[뼈 뒤집기]` 가 네 뼈 전부 `(0, len)` 에 떨어지는지 본다.

### 관절 좌표를 고칠 때

관절은 **그 높이의 불투명 픽셀 중심**에 박아야 한다. 눈대중으로 적으면
파츠끼리 반대쪽으로 밀려서 이음매가 어긋난다.
파이썬으로 파츠 박스를 가로줄마다 훑어서 중심을 재고 그 값을 쓴다.

### 옛 브라우저의 flex

`[만약에 라인스코어]` 검사는 `.wl-r i{min-width:0}` 을 **일부러 끼워 넣고** 잰다.

옛 브라우저(삼성 인터넷 구버전·옛 안드로이드 웹뷰)는 flex 항목의
`min-width:auto` 를 구현하지 않아서 **내용보다 더 줄어든다.**
그러면 이닝 칸을 담은 `<i>` 가 세 칸 폭으로 쪼그라들고, 안에 든 칸들은
그대로 밖으로 삐져나와 R·H·E 밑에 깔린다. 실제 제보 화면이 딱 이거였다.
최신 크롬에서는 `min-width:auto` 덕분에 안 나서, 그냥 열어보면 못 찾는다.

그래서 **폭이 고정된 칸을 품은 flex 상자에는 `flex:0 0 auto` 를 준다.**
`min-width:auto` 에 기대지 마라. 줄 자체는 `width:max-content; min-width:100%`
로 두고 바깥에서 `overflow-x:auto` 로 민다.

## 통짜 그림 (타자 · 투수)

v2.31.0 부터 **타자와 투수는 관절 조립을 안 한다.** 그림 한 장(`MV_FIG_SRC`)
으로 그린다. 몸이 애초에 이어져 있으니 이음매가 생길 자리가 없다.
야수 아홉 명과 포수는 그대로 파츠(`MV_SHEET_SRC`)를 쓴다 — 20px 짜리에서는
티가 안 난다.

- `MV_FIGS` 상자, `MV_FIG_ANCH` 는 `[발 중앙 x, 머리끝 y, 발바닥 y]` (상자 비율)
- `mvFig(g,k,x,y,h,u,pose,flip)` — `(x,y)` 가 **발바닥**, `h` 가 머리끝~발바닥
- `MV_FIG_POSE[pose]` = `[가로배율, 세로배율, 앞뒤밀기, 기울기, 배트각, 다리모으기]`

### 배트는 몸이 아니다

배트는 물건이라 그림에서 지우고 파츠 시트의 `bat` 을 따로 얹는다.
그래야 몸은 통짜로 두면서 스윙 때 배트만 돌릴 수 있다.
지운 자리에 생긴 구멍은 이웃 색으로 메웠다 — 안 메우면 손 언저리로 잔디가 비친다.
`arttest` 가 `[통짜 그림]` 에서 구멍 개수와 '배트가 지워졌는지'를 본다.

### 다리 모으기

그림은 스트라이드를 벌린 자세라, 세트 자세로 돌리려면 다리만 좁혀야 한다.
벨트 아래를 32조각으로 잘라 배율을 조금씩 바꿔 가며 그린다.
한 번에 확 좁히면 벨트에서 몸이 계단처럼 꺾이고, 조각이 적으면 옆선이 톱니처럼 튄다.

## 사파리 텍스트 자동 확대

사파리(특히 아이폰)는 **좁은 칸 안의 작은 글자를 제멋대로 키운다.**
전광판 글자를 8px 로 잡아놔도 아이폰에서는 부풀어서 칸을 넘쳤다.
크롬에서는 안 일어나니 열어봐도 못 찾는다.

`*{-webkit-text-size-adjust:100%}` 로 전역에서 껐다.
그리고 전광판은 `mvScore()` 가 실제 너비를 재서 글자 크기를 직접 넣는다 —
칸 하나에 두 자리가 들어가려면 글자가 칸 너비의 0.55 배여야 한다.

`boardtest` 는 `.mvb-r i u{min-width:0}` 과 `font-size:12px !important` 를
**일부러 끼워 넣어** 최악을 만든 다음, 320·360·390 (아이폰 폭)에서 숫자가
옆 칸으로 삐져나오는지 본다. `BOARD_HARSH=0` 으로 돌리면 제 크기로 검사한다.

**폭이 고정된 칸에는 `overflow:hidden` 을 준다.** 넘치더라도 겹치지 말고 잘려라.

### 던지는 팔

투수는 통짜 그림이라 그대로 두면 팔이 안 움직인다 — 매 프레임 팔 벌리고
서 있는 꼴이 된다. 그래서 **던지는 팔만 어깨에서 잘라내 따로 돌린다.**

- `MV_FIGS.parm` — 팔 조각 상자
- `MV_FIG_ARM.pit` = `[어깨x, 어깨y (투수 그림 기준), 조각 안에서의 어깨x, 어깨y]`
- `MV_FIG_POSE[pose][6]` — 팔각 (그림에 그려진 화면 153도에서 얼마나 더 돌리나)
- `MV_FIG_POSE[pose][7]` — 손에 공이 있나 (팔로스루는 0)

자를 때 주의할 것:
- **어깨 그루터기를 남긴다** (피벗 반경 20px 안쪽은 몸통에 둔다). 안 그러면
  팔이 돌아갔을 때 어깨에 구멍이 뚫린다.
- 자르고 나면 **몸에서 떨어진 작은 덩어리**(소매 외곽선 조각)가 남는다.
  연결 성분을 세서 제일 큰 것만 남기고 지운다.
- 자른 자리에 **안쪽 구멍**이 생긴다. 바깥에서 안 닿는 투명 픽셀을 찾아
  이웃 색으로 메운다. 안 메우면 그 틈으로 잔디가 비친다.
- **릴리스에서 팔이 머리를 넘어가면 안 된다.** 넘어가면 공이 모자 위에 뜬 것처럼
  보인다. 머리 왼쪽(화면 262도)에서 끝낸다.

`arttest` 의 `[통짜 그림]` 이 팔각이 자세마다 다른지, 코킹에서 팔이 실제로
78px 올라가는지, 팔로스루에서 내려오는지를 캔버스 픽셀로 잰다.

## 그라운드 홈런 배수

`simPA` 의 그라운드 홈런 확률에 **구장 배수를 그대로 곱하면 두 번 먹는다.**
배나물은 이미 3루타가 1.55배 많은데, 거기서 홈까지 도는 확률에도 1.55배를
곱하면 실제로는 2.4배가 된다. 제곱근만 먹인다.

```js
const chance=clamp(LG.ITP_BASE*(0.5+fast)*sloppy*Math.sqrt(park.d3||1), 0, 0.34);
```

기준점은 **실제 2026시즌 기록**이다 — 팀 홈런은 그라운드 홈런 한 개뿐이었고
담장을 넘어간 타구는 시즌 내내 0개였다. 그 정도로 드물어야 맞다.
`verify` 의 홈런/안타% 와 `itptest` 의 그라운드 비율을 같이 보면서 맞춘다.

## 화면 축 (투수 ↓ 공 ↓ 타자 ↓ 포수)

중계 화면은 **포수 뒤에서 마운드를 보는 시점**이다. 그래서:

- 투수는 화면 **위**(마운드), 타자는 화면 **아래**(타석), 포수는 그보다 더 아래
- 공은 위 → 아래로 간다 (`throwBall` 의 릴리스 지점이 `H*0.49`)
- **우타는 3루쪽 = 화면 왼쪽**, 좌타는 1루쪽 = 화면 오른쪽

타석 박스 좌표는 `MV_BOX` 하나뿐이다. `mvField()` 가 그걸로 그리고
`mvPaint()` 가 `MV_BOXC` 로 타자를 세운다. **좌표를 두 군데 박지 마라** —
그래서 타자가 박스 밖에 서 있는 걸 오래 못 잡았다.
`axistest` 가 이 축과 박스를 전부 좌표로 검사한다.

## 완성 포즈 그림 (v2.35.0~)

타자·투수는 **조립하지 않는다.** 그림 한 장이 곧 한 자세다.

```js
const MV_FIGS={ pit:[...], bat:[...], pitw:[...] };   // 상자
const MV_FIG_OF={ idle:'pitw', wind:'pitw', cock:'pitw',
                  rel:'pit', follow:'pit', stand:'bat', swing:'bat' };
const MV_FIG_H={ pit:52, pitw:50, bat:92 };           // 화면에서의 키
mvFig(g, pose, x, y, u, flip)                          // (x,y) 가 디딘 발
```

`mvFig` 안에는 `drawImage` 가 한 번뿐이다. 머리를 얹거나 팔을 돌리거나
다리를 눌러 좁히던 코드는 전부 걷어냈다 — **자세를 코드로 만들어내려 하면
아무리 맞춰도 사람으로 안 보인다.** 자세가 더 필요하면 그림을 한 장 더 그린다.

`axistest` 의 `[완성된 그림을 그대로 쓴다]` 가 조립 코드가 되살아나면 잡고,
`[누가 누구를 보고 있나]` 가 살색 픽셀로 방향을 잰다 —
투수는 얼굴이 크게(정면), 타자는 작게(뒤통수) 보여야 한다.

## 스윙 · 등번호 (v2.36.0~)

**타격 판정은 원래부터 제대로 있었다.** `renderSwing` 의 `judge()` 가
타이밍(`tap.t-0.90`)과 자리(`|tap - 공|`)를 둘 다 재서
`q = timing^0.85 * loc^0.70` 을 만들고, q<0.18 헛스윙 / q<0.38 파울 / 그 위 컨택이다.
문제는 **눈에 안 보이는 것**이었다 — 스윙 포즈가 준비 자세와 같은 그림이라
버튼을 눌러도 화면이 그대로였다.

`mvSwingPose(t)` 가 0~1 진행도로 [몸 회전, 앞으로 밀기, 배트 잔상]을 준다.
`mvSwing()` 이 0.44초 동안 매 프레임 다시 그린다.
`axistest` 의 `[스윙이 눈에 보인다]` 가 프레임마다 그림의 무게중심이
실제로 옮겨졌다가 돌아오는지 픽셀로 잰다.

등번호는 **그림에 박아두지 않는다.** 타자 그림에서 숫자를 지우고
`MV_BAT_NO` 자리에 `jerseyNo(pid)` 를 캔버스로 찍는다.
번호를 바꿨을 때 그림이 달라지는지도 테스트가 확인한다.

## 타석 입력 (v2.37.0~)

타석에는 **행동이 셋**이다. 셋 다 서로 다른 함수로 간다.

| 버튼 | 하는 일 |
|---|---|
| `[지켜본다]` `.pl-hit` | `judge(null)` — 안 휘두른다. 심판 판정 |
| `[스윙]` `.pl-swing` | `swingAt(null)` → `mvSwing()` + `judge({t, x,y})` |
| `[알아서 치게 둔다]` `.pl-skip` | 자동 진행 |
| 야구장 탭 (보조) | `swingAt({x,y})` — 자리까지 노리는 정밀 입력 |

**버튼 스윙의 자리**는 존 한가운데에서 공 쪽으로 55%만 따라간다.
단, 공을 존 안(0~2)으로 clamp 해서 계산한다 — **존 밖으로 빠진 공은
몸이 못 따라간다.** 그래서 한가운데 정타 / 구석 빗맞음 / 존 밖 헛스윙이
자연스럽게 나온다. 판정식(`q = timing^0.85 * loc^0.70`) 은 안 건드렸다.

`swingtest` 가 두 버튼이 색·크기·문구로 구분되는지까지 본다 —
**제일 큰 버튼이 스윙이어야 한다.**

## 구종 배분 (v2.37.1~)

던질 구종은 `pickPitch(pit, rng)` 로 뽑는다. **arsenal 에서 균등 추첨하면 안 된다.**

`arsenalOf()` 는 던질 수 **있는** 구종을 주고, `pitchMix()` 가 얼마나 던지는지를 준다.
직구가 기본 58%(제구가 나쁘면 ↑, 구위가 좋으면 ↑), 남은 몫을 변화구가 나눠 갖는다.

[버그 이력] 균등 추첨이라 5종 던지는 투수는 「변화구」 하나만 노려도 80% 가 맞았다.
노림수는 둘 중 하나를 고르는 건데 한쪽이 80% 면 게임이 안 된다.
`swingtest` 의 `[구종 배분]` 이 **어떤 투수든 변화구 총합이 50% 미만**인지,
직구가 항상 최다인지, 실제로 2만 번 뽑아도 배분대로 나오는지 검사한다.

## 수비수 글러브 손 (v2.38.0~)

중계 화면은 **포수 뒤에서 보는 시점**이라 야수는 카메라를 마주 본다.
마주 본 사람의 **왼손은 화면 오른쪽**이다. 그래서

| 던지는 손 | 글러브 | 화면에서 |
|---|---|---|
| 우투 (기본) | 왼손 | **오른쪽** — `MV_POSES.idle` (`handF:'glove'`) |
| 좌투 | 오른손 | 왼쪽 — `MV_POSES.idleL` (`handB:'glove'`) |

[버그 이력] 기본 포즈가 `handB:'glove'` 라 아홉 명 전원이 좌투로 그려졌다.
**좌우 반전으로 때우면 안 된다** — 얼굴·몸통·다리까지 뒤집혀서 경기장 방향과
어긋난다. 글러브를 어느 팔에 물리느냐만 바꾼다.

자리별 선수는 `MV_FIELD_SPOT`(3B·SS·2B·1B) 과 `fielderThrowsL(pos)` 로 찾는다.
경기 중이 아니거나 못 찾으면 우투로 본다.
`axistest` 의 `[수비수 글러브]` 가 실제로 그려서 **갈색 픽셀 무게중심이
어느 쪽인지**까지 잰다.

## 유니폼 등번호와 어깨 이음매 (v2.39.0~)

### 소매는 몸통보다 **먼저** 그린다

[제보] "양쪽 팔이 몸통과 자연스럽게 연결되지 않고 어깨에서 기괴하게 꺾여 있다"

각도도 어깨(`MV_SH`)도 회전 기준점도 멀쩡했다. **그리는 순서**가 원인이었다.
`sleeve` 파츠는 어깨를 통째로 덮는 둥근 덮개라 폭이 5.2칸인데
몸통 흰 부분은 8.5칸뿐이다. 앞쪽 팔을 몸통 **위에** 통째로 얹으니
소매가 가슴을 1.9칸(그림 20px) 덮어서 한자 「腕」 이 잘리고
어깨선이 가슴 한복판에서 꺾인 것처럼 보였다.

사람을 정면에서 보면 몸통이 어깨 안쪽을 가린다. 그래서 `mvArm(...,layer)` 로
갈랐다 — **소매는 언제나 몸통보다 먼저, 팔뚝과 손만 몸통 앞에.**

```
legB legF  →  armB(통째)  →  armF 소매만('up')  →  몸통  →  등번호  →  머리  →  armF 팔뚝만('lo')
```

### 등번호는 유니폼 그림 좌표에 박는다

[제보] "등번호가 캐릭터의 왼쪽에 렌더링된다 / [한자] [번호] 순서로 읽혀야 한다"

예전엔 몸통 밖에서 `fillText` 로 **캐릭터 좌표**에 그냥 찍었다. 그래서

1. 옷을 안 따라다녔다 (몸이 기울어도 번호는 안 기울었다)
2. 좌우 뒤집기가 두 번 걸려서 **좌투는 화면 왼쪽, 우투는 오른쪽** — 선수마다 자리가 바뀌었다
3. 한자보다 위·왼쪽이라 소매에 반쯤 걸쳐 [번호][한자] 로 읽혔다

이제 `mvBoneTx(g,'torso',...)` 로 **유니폼 그림의 로컬 좌표계**를 걸고 찍는다.
자리는 `MV_FIELDER_NO`(그림 안 비율)에 있다 — 단추선(가로 0.52)보다 오른쪽,
한자 아래단(세로 0.59)보다 아래. 색은 `mvInkFor(u)` 가 유니폼 색상각을 따라간다.

타자·투수는 통짜 그림이라 별도다 — `MV_BAT_NO`(그림 픽셀). 등을 보이고 서 있어서
자리가 다르다. 이름을 갈라 둔 이유가 이것이다.

### 무엇으로 잡나

`numtest` (크로미움 필요). 실제로 그려서 픽셀을 센다.

| 검사 | 어떻게 |
|---|---|
| 앞 소매가 한자를 안 덮는가 | 팔까지 그린 것과 몸통만 그린 것의 한자 띠 잉크 픽셀 수를 **양쪽으로** 비교. 소매 천도 같은 네이비라 가리면 픽셀이 오히려 늘어난다 |
| 번호가 한자 오른쪽 아래인가 | 잉크 무게중심이 몸통 중앙보다 오른쪽, 한자 아래단보다 아래 |
| 좌투·우투 자리가 같은가 | `flip` 을 켜고 끄고 두 번 그려서 무게중심 차이 4px 미만 |

옛 코드로 돌리면 세 개 다 실패한다.

## 다시 굴리기 차단 (v2.40.0~)

[제보] "새로고침 하면 못 온다고 하는 사람 바뀜.
새로고침을 이용해서 이길 때까지 돌리는 게 가능해요"

원칙은 하나다. **결과를 정하는 난수는 저장된 상태에서만 나온다.**
`Math.random()` 이나 `Date.now()` 로 굴린 것은 새로고침하면 다시 뽑히고,
다시 뽑힐 수 있으면 마음에 들 때까지 굴릴 수 있다.

| 어디 | 예전 | 지금 |
|---|---|---|
| 번복 불참 (`buildKakaoPre`) | `Math.random` | `ST.seed + round + weekSeq` 로 고정 |
| 경기 (`makeLive`) | `ST.seed + round + Date.now()%100000` | `ST.seed + round + weekSeq` |
| 라인업 화면 '카톡 발표' | 잠금 없음 — 계속 누를 수 있었다 | `ST.announced` 면 '카톡방 열기' |
| 경기 화면 '사정해본다' | 잠금 없는 옛 판 (확률도 45% 로 달랐다) | `shortHandedButtons` 한 벌로 통일 (`begAsked`, 20%) |

우취로 한 주를 다시 굴리면 `weekSeq` 가 올라가니 시드도 같이 바뀐다.
그건 게임이 의도한 재추첨이라 그대로 둔다.

직접 지휘에서 내 선택이 달라지면 결과도 달라진다 — 그건 실력이라 막지 않는다.
막는 건 **같은 선택으로 다른 결과를 얻는 것**뿐이다.

### 시뮬레이션 테스트를 쓸 때 주의

경기 난수가 `ST` 에서 나오므로 `makeLive()` 를 반복 호출해도 **같은 경기**가 나온다.
확률을 재는 테스트(`mgrtest` 의 물통차기 3%, `playtest` 의 판단 횟수 비교)는
루프 안에서 `ST.seed` 를 돌려야 한다. 안 돌리면 4000경기가 한 경기다.

### 무엇으로 잡나

`scumtest`. 같은 주 상태를 되돌려 놓고 두 번·세 번 굴려서 결과가 같은지 본다
(새로고침한 것과 같은 조건이다). 옛 코드로 돌리면 네 개가 실패한다.

아무도 안 빠지는 주면 "늘 같다" 가 우연히 통과하므로, 실제로 빠지는 사람이
나오는 주를 먼저 찾아서 거기서 검사한다.

## 명예의 전당 (v2.41.0~)

[제보] "명예의 전당 헌액자도 조건 더더더 까다롭게 해야할듯. 경기만 나오면 누구나 하겠네.
그리고 구단별 단일 시즌 최고 기록도 규정타석 규정이닝 맞게 되는 거지?"

### 헌액

**자격**(통산 120경기 또는 150이닝)을 넘긴 사람만 심사하고,
그중 **업적 하나**를 해내야 들어간다. 출전 수는 자격일 뿐 업적이 아니다.

숫자는 눈대중이 아니라 **세 시즌을 실제 경기 흐름으로 돌려서** 잡았다
(결장·부상·로테이션 포함). 한 시즌에 이 정도가 쌓인다:

| | 한 시즌 |
|---|---|
| 주전 야수 | 22경기 · 안타 40 · 타점 25 · 도루 33 · 홈런 1~2 |
| 에이스 | 15등판 · 39이닝 · 39탈삼진 · 4승 · 개인 타이틀 6개 |

여기에 맞춰 **여덟 시즌쯤 최고로 뛰어야 하나 걸리는** 선으로 올렸다.
홈런만 반대로 20 → 12 로 낮췄다 — 20개는 열다섯 시즌짜리라 있으나 마나였다.

'구단 단일 시즌 최고 기록 보유' 도 헌액 사유로 넣어봤다가 뺐다.
1년차가 끝나면 그 열두 부문을 잘하는 서너 명이 통째로 나눠 갖는다.
한 시즌만 뛰고도 절반을 들고 있으니 까다롭게 하려는 것과 정반대다.

### 규정타석 · 규정이닝

비율 기록(타율·OPS·방어율)은 **원래부터** `qualPA`/`qualOuts` 를 걸고 있었다.
누적 기록(안타·홈런·이닝…)에 최소 기준이 없는 것도 맞다.

고친 건 **진행 중인 시즌의 기준 경기 수**다. 예전엔 그때까지 치른 경기로
기준을 잡아서 개막 3경기에 6타석 4안타면 타율 .667 이 구단 기록으로 올라왔다.
이제 `gMark(y)` 가 진행 중인 시즌은 **다 치렀을 때**를 기준으로 잡는다.
시즌이 끝나야 구단 기록에 낄 수 있다는 뜻이고, 그게 규정타석의 원래 뜻이다.

그 해 팀 리더(`gOf`)는 지금까지 치른 만큼으로 그대로 둔다 — 시즌 내내
'타율 1위 없음' 이면 화면이 죽는다.

### 무엇으로 잡나

`hoftest`. 출전만 많은 사람(400경기 120안타)이 안 들어가는지,
업적을 넘긴 사람은 들어가는지, 근속 미달이 걸러지는지를 실제 화면의
헌액자 줄로 확인한다.

## 우승 세리머니 (v2.42.0~)

[요청] "우승하면 뭔가 축하하는 그런 모션 나오면 좋을듯"

한 시즌을 다 돌려야 한 번 볼까 말까 한 장면이라 여기만큼은 요란해도 된다.
색종이 44장이 쏟아지고, 트로피가 튀어나와 둥실거리고,
그날 결승에 나온 사람들이 헹가래를 친다. 내가 고른 선수만 더 높이 뛴다.

**전부 CSS 애니메이션이다** — 캔버스도 타이머도 안 쓴다. 느린 기기에서
버벅이지 않고, 화면을 닫으면 그걸로 끝이다. 움직임 줄이기
(`prefers-reduced-motion`)는 파일 맨 위 전역 규칙이 이미 animation 을 끈다 —
여기서 또 쓰면 `lintcheck` 의 중복 셀렉터에 걸린다.

`@keyframes` 의 `0%`/`100%` 도 줄 맨 앞에 오면 셀렉터로 세어진다.
새 keyframes 는 **한 줄로** 쓰거나 들여쓰기 없이 이어 붙여라.

뜨는 곳은 두 군데뿐이다.
- `commitGame` — 결승을 이긴 그 순간 (`ST.playoff.place==='우승'` 일 때만)
- 시즌 종료 화면의 「우승 세리머니 다시 보기」 — 그때 화면을 못 본 사람용

`champtest` 가 색종이가 제각각 떨어지는지(자리·시차가 다 다른지),
내 선수 표시가 하나뿐인지, 준우승에는 안 뜨는지까지 본다.

## WAR (v2.48.0~)

[요청] "투수 war 타자 war 따로 해서 선수 합쳐도 되고"

### 왜 MLB 상수를 못 쓰나

이 리그는 **7이닝제에 팀당 9.5득점**(9이닝 환산 12.2점)이다.
빌려온 상수는 전부 다른 세계의 숫자다.

| | 예전(MLB) | 지금(실측) |
|---|---|---|
| 승리당 득점 | 10 | **16.1** |
| 단타 | .89 | **.781** |
| 홈런 | 2.10 | **1.585** |
| 타석당 득점 | "약 0.17" 로 박아둠 | **0.253** |
| 대체선수 가산 | 0.030 (근거 없음) | **0.0718** |

홈런/단타 비율이 MLB 3.0배 → 여기 2.0배로 좁혀지는 게 핵심이다.
타율 .450 짜리 리그에서는 어차피 뒤 타자가 불러들이니 혼자 넘기는 값이 싸진다.
**이 게임에서는 파워보다 출루가 값지다** — WAR 이 그걸 반영해야 한다.

### 대체선수 수준을 어디로 잡았나

처음엔 용병(조창익·박재민·최민서)으로 잡으려 했다. 공짜로 부르는 사람이니
정의에 맞아 보였다. 재보니 **용병 아홉으로 채운 팀은 경기당 0.94득점, 22경기 0.1승**.
대체선수가 아니라 재난이다. WAR 의 대체수준은 "한 명 빠졌을 때 공짜로 메우는 수준"
이지 "전원을 그걸로 채운 팀" 이 아니라서, 그대로 쓰면 모두의 WAR 이 부풀어 오른다.

그래서 **이 리그 하위 3팀(SEW KOREA·마이볼·보스, 승률 .182)** 으로 잡았다.
실제로 존재하는 팀 중 제일 아래고 "이보다 못하면 팀이라 부르기 어렵다" 는 선이다.

10시즌 검산 — 대체수준 팀은 22경기에서 **5.1승** 자리에 놓인다.
하위 3팀 실측 4.0승보다 살짝 높은데, 수비(실책) 항만 대체수준이 아니라
리그 평균 기준이라 그렇다. 1승 차이라 그대로 뒀다.

### 투수는 자책(ER), 수비는 실책

v2.47.0 이 자책점을 야구 규칙대로 계산하고 실책을 개인에게 달면서
팀 실점을 **투수(자책) + 야수(비자책)** 로 겹치지도 새지도 않게 나눌 수 있게 됐다.
이 리그는 실점의 **28%가 비자책**이다 — 실제 2026시즌에도 김한규가
25실점 중 자책 8점이었다. 실점(RA) 기준으로 하면 그 17점이 통째로 투수 탓이 된다.

수비는 **실책만** 센다(1개 = 1.044점 = 단타 .781 + 아웃 .263).
범위·송구는 게임이 아예 안 재니 넣을 수가 없다. 반쪽인 걸 알면서 넣은 이유는
비자책이 28%나 되고, 김선호의 실책 8개 같은 게 기록에 남아야 이야기가 되기 때문이다.

### 표본

한 시즌 22경기, 주전도 90타석 남짓이라 WAR 이 크게 흔들린다.
**숫자 옆에 항상 표본(타석·이닝)을 같이 적고**, 규정 미달은 흐리게 보여준다.
숨기지 않는다 — 안 보이면 오해가 더 커진다.

### 무엇으로 잡나

`wartest`. `verify.js` 처럼 **매번 엔진에서 상수를 다시 재서** 박아둔 값과
맞는지 본다. 엔진 밸런스를 건드리면 WAR 이 조용히 틀려지는데,
화면에는 여전히 그럴듯한 숫자가 찍혀서 아무도 모르기 때문이다.

상수를 MLB 값으로 되돌려 놓으면 4개가 빨간불이 된다 (확인함).

## 분탕형 — 내쫓은 선수가 상대 팀으로 (v2.49.0~)

[계획서] 플레이어 유형 3번(분탕형)의 처방:
"내쫓은 선수가 다른 팀 가서 우리 상대로 나오게 하기"

재료는 이미 다 있었다 — `ST.unhappy` → 이적 요구 → `leaveTeam()` 파이프라인도,
시즌을 넘어가도 이월되는 `ST.leftPlayers` 기록도. 나간 사람이 그냥
사라질 뿐이었다. 그래서 불만을 방치하는 데 아무 대가가 없었다.

### 흐름

```
leaveTeam() → pickNewTeam()  어느 팀으로 갈지 (시드 고정)
            → placeExile()   상대 로스터에 꽂는다
normalizeState() → applyLeftPlayers() → placeExile()   매 부팅마다 다시
renderGame / renderHome → exiledOn(팀id)  경기 전에 이름을 짚어준다
```

### 제일 약한 고리 — 부팅마다 다시 꽂아야 한다

**선수 능력치는 세이브에 안 들어간다.** 매 부팅마다 `WWZW` 상수에서
로스터를 새로 만든다. 그래서 런타임에 로스터를 바꾼 건 전부
`normalizeState()` 에서 다시 적용해야 한다 (전역 복귀한 송정민이
v2.46.2 에서 사라진 것도 같은 이유였다).

`placeExile()` 은 **여러 번 불려도 한 번만 들어가야** 한다.
`normalizeState()` 는 부팅·세이브 로드·새 시즌마다 돈다.
`exiletest` 가 `normalizeState()` 를 세 번 연달아 부르고 중복을 센다.

선수 객체는 우리 로스터에서 빼기 **전에** 잡아둔 것을 쓴다.
못 잡았으면 `WWZW` 상수에서 다시 만든다 — 세이브만 있고 메모리엔
없는 상태(새로고침 직후)가 그 경우다.

### 재추첨 방지

가는 팀은 `makeRng(seed + clayHash(pid) + seasonNo)` 로 정한다.
새로고침해서 마음에 드는 팀이 나올 때까지 돌릴 수 없다 —
v2.40.0 에서 막은 것과 같은 이유다.

### 무엇으로 잡나

`exiletest`. 내보내고 → 상대 로스터에 들어갔는지 → 상대 라인업에 섰는지 →
새로고침 후에도 남아 있는지 → 중복으로 안 들어가는지 →
화면에 뜨는지 → **실제로 우리 경기에 타석에 서는지**까지 본다.

## 화면을 내려놓았을 때 (v2.49.1~)

[제보] "갤럭시 쓰는 애가 배터리가 빨리 닳는다" 의 남은 조각이다.

v2.43.0 이 브금(`BGM.onHide`/`onShow` + `audioSession` 을 아이폰에서만)을
재웠는데, **직접 지휘 중계 타이머**는 그대로 돌고 있었다 —
0.52초마다 `step()` 하고 DOM 을 다시 그린다.

자동 진행은 `while` 로 즉시 끝내니 이 타이머를 안 탄다.
이 타이머는 **직접 지휘에서 판단과 판단 사이를 이어주는 용도**다.
그래서 "안 보는 사이에 경기가 끝나 있다" 는 자동 진행에는 해당이 없다 —
처음에 그렇게 적었다가 고쳤다.

가드는 `return` 만 한다. `clearInterval` 을 하면 돌아왔을 때 영영 안 살아난다.

### 검사가 왜 소스 위주인가

`bgtest` 의 진짜 판별기는 소스 검사다. 동작 검사('내리면 멈춘다')는
**HEAD 에서도 우연히 통과한다** — 장면 애니메이션이 한 타석에 몇 초씩
걸려서, 안 고쳐도 그 3.5초 동안 마침 안 움직일 수 있다.
'돌아오면 다시 굴러가나' 는 아예 검사하지 않는다. 같은 이유로 짧은 창에서는
HEAD 에서도 진행이 안 잡혔다 — 대신 타이머 핸들이 살아 있는지를 본다.

## 맞혔는데 삼진 · 투구수 (v2.50.0~)

### clamp 하한이 보정을 도로 끌어올리고 있었다

[제보] "투스트라이크에서 잘맞았는데 삼진이라고 나옴"

배트에 맞으면 삼진·볼넷이 나올 수 없게 `consumePlayMods` 가
`{k:0.0008, bb:0.0008}` 을 넘기고 있었다. 그런데 `simPA` 가

```js
let kR = clamp(LG.K * ... * (mods.k||1), 0.01, 0.55);
```

**하한 0.01** 로 잘라낸다. 0.0008 을 곱하든 0 을 곱하든 최소 1% 는 남는다는 뜻이다.
실측하니 컨택 100번에 **삼진 0.95% · 볼넷 1.12%** 가 그대로 나왔다.

하한은 리그 전체 비율이 무너지지 말라고 둔 것이지 개별 보정을 무시하라는 게 아니다.
그래서 `mods.inPlay` 플래그를 만들어 `kR=bbR=hbpR=0` 으로 **못을 박았다.**
확률로 미는 방식은 언젠가 또 새어 나온다 — 실제로 새어 나왔다.

`nptest` 가 2스트라이크 컨택 30000번을 굴려서 삼진·볼넷이 **정확히 0** 인지 본다.
고치기 전 코드로 돌리면 빨간불이 된다.

### 투구수는 결과에서 되짚는다

[요청] "투구수 현황 표시도 가능한가?"

엔진은 공을 한 개씩 굴리지 않는다. `simPA` 가 타석 결과를 한 번에 낸다.
그래서 `pitchesForPA(type, rng)` 가 **결과에서 되짚어** 굴린다.
야구 규칙상 못 어기는 것만 지킨다 — **삼진 최소 3구 · 볼넷 최소 4구.**

직접 지휘로 손으로 친 타석은 `pitchResult` 가 공 하나마다 불리므로
**진짜 던진 개수**(`_paPitches`)를 쓴다. 그쪽이 있으면 그걸 우선한다.

실측 **타석당 4.07~4.11구**. 볼넷이 15.7% 나 되는 리그라 카운트가 깊어진다.

보이는 곳은 세 군데다.
- 경기 중 `#livectl` — 투수 교체 버튼 바로 위 (교체 판단에 제일 필요하니까)
- 박스스코어 투수표 `투구` 칸
- 시즌·리그 기록(`ST.pit` / `ST.lgPit` 의 `np`)

`#livectl` 은 `.btn-row(display:flex)` 라 그냥 넣으면 버튼 옆에서 눌려 찌그러진다
(122px 로 줄어 3줄로 접혔다). `flex-wrap:wrap` + `flex:1 0 100%` 로 제 줄을 갖게 했다.

옛 세이브에는 `np` 칸이 없다. `normalizeState` 가 0 으로 채운다.

## 구장 규격 · 타구 비거리 (v2.51.0~)

[제보] "경기 규격 좌 중 우 순이야 … 배나물: 98 122 98"
[요청] "칠때마다 비거리도 적어주고 파울이나 땅볼 이런거에도 말이지"
[요청] "그 움직이는 애니메이션 모션 비거리에 따라 확실히 달리해주고"

### 규격

`PARKS[].dim = [좌, 중, 우]` (m).

| 구장 | 좌 | 중 | 우 |
|---|---|---|---|
| 배나물 | 98 | 122 | 98 |
| 성곡 | 93 | 105 | 93 |
| 신길 | 95 | 115 | 95 |

**성곡이 두 번 적혀 있고 신길이 빠져 있었다.** 구장도 셋, 숫자도 셋이라
하나는 신길로 봤고, 어느 쪽이 성곡인지는 기존 구장 성격에 맞췄다 —
성곡은 원래 홈런이 제일 잘 나오는 구장(`hr:2.30`)이라 더 짧은 93/105/93 을 줬다.
**바뀐 거면 `PARKS` 의 두 `dim` 만 서로 바꾸면 된다.**

구장 배수(`hr`·`d2`·`d3`…)는 실제 기록에 맞춰둔 값이라 안 건드렸다.
규격은 비거리를 "담장까지 몇 m 였나"로 말해주는 데 쓴다.

`fenceAt(park, ang)` 이 각도별 담장 거리를 준다. 라인 쪽으로 갈수록
급히 짧아지게 `pow(|t|,1.35)` 로 보간한다.

### 비거리는 결과에서 되짚는다

엔진은 타구 방향도 비거리도 모른다 — 투구수(v2.50.0)와 같은 문제다.
그래서 `battedBall(seq, kind, park, bats, itp)` 이 **결과에서 만든다.**

규칙 두 개가 절대적이다.

1. **결과와 어긋나면 안 된다.** 홈런은 `담장+1~14m`, 그라운드 홈런은
   `담장-5~0m`, 나머지는 전부 담장 안. `disttest` 가 84000개를 굴려서
   한 개라도 어긋나는지 본다.
2. **로그와 화면이 같은 숫자를 써야 한다.** 로그엔 96m 라고 적고 화면에선
   딴 데 떨어지면 바로 들킨다. 그래서 **경기 난수를 안 쓴다** —
   타석 번호로 자기 난수(`bbRng`)를 돌린다. 어디서 몇 번을 불러도 같은 값이
   나오고, 경기 시드가 밀려서 경기 자체가 바뀌는 일도 없다.

### 화면 환산 — 내야와 외야를 따로

도식의 내야는 실제보다 크게 그려져 있다. **2루가 실제 38.8m 인데 104px**
(선형이면 64px). 보기 좋으라고 그렇게 잡힌 것이다.

그대로 선형 환산하면 **15m 땅볼이 홈플레이트 위에 떨어지고 야수가 홈으로
달려온다.** 그래서 `psMtoPx` 가 내야(0~38.8m→0~104px)와
외야(38.8m~담장→104px~담장px)를 따로 매핑한다. 그림 비율은 그대로 두면서
거리는 단조증가한다.

### 애니메이션

체공 시간 `300 + m*5.4` ms, 높이 `6 + m*0.42` (땅볼은 3 고정).
**결과 종류가 아니라 거리가 정한다** — 예전엔 "2루타면 무조건 162px" 라
95m 짜리와 115m 짜리가 화면에서 똑같이 날아갔다.

담장도 `psField(g, park)` 가 구장 모양대로 그린다. 원이 아니다.

### 방향

`psAngle` 의 폭이 좁아서 **우타가 우측으로 아예 안 쳤다** — 실측
우측 0.0% · 우중간 1.1% 였다. 4부 타자들은 그렇게 안 친다(실제 기록에도
"좌측 44% 로 균형 타구" 같은 메모가 있다). 난수를 둘 더해(삼각분포)
가운데를 두껍게 하고 꼬리를 남겼다 — 지금은 당긴 쪽 51% · 반대쪽 18% 다.

## 안 보이던 걸 보이게 (v2.51.1~)

[제보] "투구수랑 왜캐 불참율이 적어졌네 라인업 발표하고 나서 불참하네
버그인지만 확인 좀 뭐 확률상 다 참석하는 거일 수도 있으니깐"

**둘 다 버그가 아니었다.** 숫자는 멀쩡했고 화면이 문제였다.
이런 제보는 고치기 전에 **먼저 재봐야 한다** — 안 그러면 멀쩡한 확률을 건드린다.

### 불참 — 확률이 아니라 자리가 문제였다

400주 실측:

| | 주당 | 한 명이라도 빠진 주 |
|---|---|---|
| 발표 전 결장 | **2.34명** | **92%** |
| 발표 후 번복 | 0.46명 | 38% |

선수별로도 `META.attend` 설정값과 정확히 맞았다
(송승민 attend .54 → 기대 22.1% / 실측 20.5%).

**발표 전이 다섯 배 많은데 체감은 반대**였던 이유:

- 결장 카드가 날씨·음주·연락·농구 카드 사이에 섞여 홈 화면 **4000px 아래**에 있었다
- 발표 후 번복은 `lineupDirty` 로 라인업을 잠가서 **길을 막는다** — 못 지나친다

그래서 소식 맨 위에 요약(`.out-sum`)을 올리고, 결장 카드를 다른 소식보다
앞에 놓고, 경기 카드에도 한 줄(`.out-line`) 넣었다.

### 투구수 — 한 명 것만 보여주고 있었다

8경기 실측: **경기당 팀 합계 156구 · 등판 2.5명 · 선발 3~4이닝.**
`checkPitcherChange` 의 설계 의도("사회인야구는 선발이 3~4이닝 던지고
내려간다")와 맞고 실제 2026시즌 송승민 등판당 2.9이닝과도 맞는다.

화면이 **지금 던지는 사람 것 하나**만 보여줘서, 교체하면 0구부터 다시
세니 "팀이 90구 던졌는데 12구" 로 보였다. 팀 누계를 같이 적는다.

### 측정할 때 빠졌던 함정

프로브에서 `L.pending=null; L.step()` 으로 돌렸더니 **투수 교체 요청이
매번 버려져서** 송승민이 144구씩 완투하는 것처럼 나왔다.
`checkPitcherChange` 는 유저 팀이면 `pending` 을 세우고 물어본다.
자동으로 돌리려면 `ST.subCfg.pitchAsk=false` 를 주거나
`L.applyDecision('change')` 를 해줘야 한다.


### 같은 이름으로 함수를 두 번 쓰면 뒤엣것이 이긴다 (2.82.0)

`traintest2` 가 다섯 건씩 **매번 똑같이** 실패하고 있었다 — 「적합도 상승」
「훈련한 포지션에 실제로 설 수 있다」 「posTrain 보존」. CLAUDE.md 에는
`traintest2` 가 확률 기반 간헐 실패로 적혀 있어서 그동안 그런 줄 알고 넘어갔다.
**세 번 돌려서 세 번 다 같은 것이 실패하면 그건 간헐이 아니다.**

원인: `posFit` 이 파일에 **두 번** 선언돼 있었다.

```
3298줄  function posFit(pid,pos)   ← 진짜. posTrained(훈련값) + posTrainCap(좌투 금지)
15646줄 function posFit(pid,pos)   ← v2.57.0 에서 새로 쓴 짧은 것. META 만 본다
```

함수 선언은 **뒤엣것이 앞엣것을 덮는다.** 그래서 v2.57.0(스물다섯 판 전)부터
포지션 훈련을 아무리 해도 적합도가 1도 안 올랐고, 좌투수도 포수를 봤다.

`lintcheck.js` 는 CSS 셀렉터와 HTML id 중복은 잡지만 **함수 이름 중복은 안 잡는다.**
새 함수를 쓰기 전에 `grep -n "^function 이름" index.html` 로 한 번 세라.

### HEAD 기준선부터 재라 — 다시 확인된 것

이번에 `traintest2`(5건)와 `whatiftest`(2건)가 같이 떴다. 새로 짠 코드 탓인가 싶어
`git stash` 로 되돌려 HEAD 에서 재보니 **똑같이 5건·2건**이었다. 둘 다 이전부터
깨져 있던 것이라 새 작업을 뒤지는 시간이 통째로 절약됐다.

그런데 둘의 성격은 정반대였다.

- `traintest2` — **코드가 틀렸다.** 위의 posFit 중복. 고쳤다.
- `whatiftest` — **테스트가 뒤처졌다.** 라인스코어가 `<i><u>` 였다가 `<u>` 로 바뀌었고,
  한 줄짜리 라인업 요약(`.wi-lu`)은 그 자리에서 직접 짜는 편집기로 대체됐다.
  화면은 멀쩡했다. 테스트를 고치고 안 쓰는 CSS 를 지웠다.

**"오래 실패하던 것"은 둘 중 하나다.** 고칠 것과 지울 것을 가르는 데는
화면을 직접 열어보는 수밖에 없다.

### 구경만 하는 화면이 데이터를 바꾸면 안 된다 (2.82.0)

선수단 둘러보기에서 상대 팀 예상 라인업을 그리려고 `aiLineup(team)` 을 불렀다.
그 함수는 **끝에 `team._slotDef` 를 덮어쓴다** — 그 팀의 수비력이다.
뎁스차트 한 번 열어봤다고 상대 팀 수비가 바뀌면 안 된다.

값 자체는 결정론적이라 결과가 달라지진 않았지만, 규칙으로 못 박는다.
**보기 전용 화면은 사본만 만진다.** `rvFakeTeam()` 이 그 사본이다.
테스트도 그걸 잰다 — 「구경만 해도 상대 팀 수비력이 안 바뀐다」.

같은 이유로 뎁스 계산도 `depScore`(우리 팀 META·ST.ca 를 본다)를 안 쓰고
선수 객체만 보는 `rvScore` 를 따로 뒀다. `posFit` 은 상대 팀 선수에게
META 가 없어서 **전부 40** 을 돌려준다 — 그대로 쓰면 스물세 팀 뎁스차트가
글자 하나까지 똑같이 나온다. 그것도 테스트로 잰다(「팀마다 뎁스가 다르다」).

### 확률로 도는 것을 '마지막 값' 으로 재지 마라 (2.82.0)

`umptest` 의 「항의 결과가 심판 성향에 남는다」가 여덟 번에 한 번씩 실패했다.
심판 성향은 항의할 때마다 **+1 / -1 로 걷는다.** 40번 걸으면 우연히
출발점(0)으로 돌아오는 경우가 12% 쯤 된다. 「끝 값이 0 이 아니다」로 재면
멀쩡한 코드가 여덟 판에 한 번 빨간불을 켠다.

**걸었는지를 재라, 어디 서 있는지를 재지 말고.** 도중에 한 번이라도
움직였으면 성향이 남는 것이다.

### 변경 내역에 'undefined' 라고 쓰면 테스트가 잡는다 (2.82.0)

`vernotetest` 는 업데이트 안내 카드에 `undefined` 나 `NaN` 이 찍히는지 본다.
템플릿에 값이 안 들어가면 사람이 바로 보는 자리라 그렇다.

2.81.0 변경 내역에 「투수 능력치가 전부 undefined 가 됐다」고 **버그 설명을**
써놨더니 그 검사에 걸렸다. 테스트가 틀린 게 아니다 — 검사 문자열을 느슨하게
풀면 진짜 렌더 사고를 놓친다. **변경 내역 쪽 말을 바꿨다**("그 값이 통째로 비었다").

### 라인업은 네 함수가 줄줄이 만든다 — 앞의 하나만 고치면 안 된다 (2.82.0)

"포수를 골랐는데 팀 가니깐 2루를 시킨다" 는 제보를 v2.79.0 에서 고쳤다고 했는데
안 고쳐져 있었다. 재보니 이렇다.

```
recommendLineup()      → C      ← v2.79.0 에서 고친 곳. 여기까진 맞았다
optimizePositions()    → SS     ← 적합도 순으로 다시 깔면서 밀어냄
applyDHRule()          → DH     ← 지명타자로 보냄
fixLineupPositions()   → ?      ← 마지막으로 또 한 번 정리
```

**라인업을 만드는 함수는 하나가 아니다.** 이 넷은 항상 이 순서로 붙어서 불린다
(`recommendLineup(); optimizePositions(); applyDHRule(); sanitizeRotation();`).
앞에서 무슨 결정을 하든 뒤엣것이 자기 규칙대로 다시 깐다.

그래서 자리를 **정하는** 게 아니라 다 끝난 뒤에 **되돌린다** — `pinMyPos()` 를
`optimizePositions` 끝과 `applyDHRule` 맨 끝(fixLineupPositions 뒤)에서 부른다.
한 명만 옮기면 자리가 겹치니 그 자리에 있던 사람과 통째로 맞바꾼다.

핀을 안 거는 경우도 규칙이다 — 오늘 내가 선발 투수(마운드가 먼저),
벤치로 밀림, 2군, 감독 모드. `pinnedPos()` 가 그 판단만 한다.

**재는 법**: 여덟 자리를 전부 돌려보고, 매번 자리 중복이 없는지와
`legal()` 을 같이 본다(`postest.js`). 한 자리만 재면 우연히 통과한다 —
실제로 C·SS·CF·1B 네 개가 전부 2B 로 나오고 있었다.

### 화면이 결과를 못 따라가면 순간이동이 된다 (2.83.0)

"2루까지 간다 누르면 갑자기 순간이동 하냐" — 진짜였다. 계산을 재보면 이렇다.

`runMs` 는 **한 베이스에 걸리는 시간**이다. 그런데 송구 창에 맞추는 줄이

```js
runMs = Math.min(runMs, Math.max(700, T_THROW-160));   // ← 베이스 수를 안 봤다
```

2루타면 두 베이스를 가야 하니 실제 도착은 `runMs×2`. 예를 들어 100m 짜리
2루타에서 내가 2루에 닿는 시각은 4300ms 인데 송구가 끝나는 시각은 2940ms 다.
그러면 마지막 한 베이스 구간이 `max(220, 2940-4300)` = **220ms** 로 눌린다.
0.2초 만에 2루에서 3루로 날아가니 순간이동으로 보이는 게 당연하다.

고친 것 세 가지:
1. `(T_THROW-160)/base` — **베이스 수로 나눠서** 딴 베이스에 송구 직전에 닿게
2. 마지막 구간에 바닥 420ms — 그 밑으로는 뛰는 걸로 안 보인다
3. 한 베이스 더 갈 때는 송구 시간을 460 → 820ms 로. 더 먼 데로 던지니 물리적으로도 맞고,
   그만큼 마지막 구간이 길어진다

**재는 법**: 1·2·3루타 각각에 대해 마지막 구간 길이를 계산해서 420ms 밑으로
안 떨어지는지 본다(`hrtest.js`). 눈으로 보면 "빠른가?" 정도라 못 잡는다.

### 로테이션은 능력치 순이라 아무나 올라간다 (2.83.0)

고교에서 **타자를 골랐는데 선발 투수로 나가고 있었다.** `aiRotation` 은
그 팀 투수를 stf 순으로 정렬해서 준다. 고1 팀에서 내 능력치가 제일 높으면
자동으로 1선발이 된다 — 내가 던지겠다고 한 적이 없는데.

`hsRotation(team, meId)` 은 "내가 던지면 맨 앞으로" 만 처리했지
"안 던지면 빼라" 는 없었다. 세 번째 인자(`offId`)를 받아서 뺀다.

같은 이유로 **이도류가 포수를 보는 날에도 마운드에 서 있었다.**
포수가 던질 수는 없다. 오늘 던지는지(`hsIPitch`)를 먼저 정하고,
그 결과로 라인업 자리와 로테이션을 **같이** 정한다.

**재는 법**: 세 역할(`pit`/`two`/`bat`) × 고교 전 경기를 돌려서
`라인업 자리`와 `선발 여부`가 서로 안 어긋나는지 본다.
`bat` 인데 선발이면 실패, `two` 인데 던지는 날 자리가 포수면 실패.

### 이야기 인덱스와 경기 번호는 다르다 (2.83.0)

이도류를 한 경기 걸러 던지게 하려고 `i%2` 를 썼는데, `i` 는 **이야기 인덱스**다.
입부·마지막날처럼 경기가 없는 장면(`noGame`)이 섞여 있어서 실제 경기 인덱스는
1·3·6·7·10·12·13 처럼 띄엄띄엄하다. 홀짝을 그대로 쓰면 일곱 경기 중
**한 번만** 던지는 판이 나온다(실제로 그렇게 나왔다).

앞에서 `noGame` 이 아닌 장면을 세서 **몇 번째 경기냐**로 갈랐다.
결과: 7경기 → 4경기 마운드 · 3경기 포수.

### 화면에서 숫자를 새로 지어내지 마라 (2.83.0)

홈런 비거리를 붙이면서 화면 쪽에 식을 하나 만들었다.

```js
const hrDist = Math.round(Math.max(95, 112 + m*0.55));   // → 189m 홈런
```

`m` 은 **이미 엔진이 계산한 비거리**(`battedDist`: 담장+1+u×13m)인데
거기에 또 식을 씌웠다. 사회인야구에 189m 짜리 타구는 없다.
`Math.round(m)` 로 바꾸니 97~129m — 담장 기준으로 말이 되는 범위다.

다시보기 화면(`psPlay`)은 원래 `d.bb.m` 을 쓰고 있었으니, 그대로 뒀으면
**같은 홈런이 두 화면에서 다른 숫자로 나올 뻔했다.**
중계 로그에도 그 값이 들어간다. 숫자는 한 군데서만 만든다.

### 포수의 수비는 타구가 아니다 (2.83.0)

"포수 수비도 없는데" 라는 제보를 받고 `DEF_ZONE` 을 봤더니 `C:[0,0]` 이고
`defZoneHit` 이 `if(pos==='C') return false;` 로 막고 있다. 의도한 설계다 —
포수한테 타구가 오는 게 포수의 일이 아니다. 포수는 **도루 저지**(`kind:'throw'`)로 나온다.

그런데 고교와 2군은 그 판단을 받는 분기가 없었다(고교는 수비 자체가 없었고,
2군은 `defplay` 만 받았다). 그래서 **포수를 고르면 수비가 통째로 안 나왔다.**
설계는 맞는데 그 설계를 태우는 길이 두 군데에서 끊겨 있던 것이다.

**교훈**: 자리마다 나오는 장면이 다르면, **그 자리로 골랐을 때 실제로 뜨는지**를
경기 종류별로(본편·고교·2군) 각각 재야 한다. 한 군데서 되면 다 되는 게 아니다.

### BABIP 이 .449 인 리그에서는 배수가 1 근처면 다 안타다 (2.83.0)

"유인구로 존 밖으로 뺐는데 안타 맞을 확률이 너무 높다" 는 제보를 재봤다.

문제는 두 겹이었다.

**① 배트에 맞기만 하면 어디에 던졌든 품질 0.55 였다.**
```js
const res=LIVE.pitchResult(kind, kind==='contact'?0.55:null, type);   // ← 고정
```
한가운데 몰린 실투나, 발끝까지 쫓아나가 억지로 갖다 댄 유인구나 같은 값.
유인구를 던질 이유가 사라진다.

**② 품질이 결과에 거의 영향을 안 줬다.**
```js
out.babip = 1 + (q-0.5)*0.30;     // q 0.2 → 0.91배 · q 0.8 → 1.09배
```
±9% 다. 그런데 이 리그의 인플레이 타구는 **절반이 안타로 빠진다**(BABIP .449,
4부 수비라 원래 그렇게 캘리브레이션돼 있다). 배수가 1 근처면 뭘 쳐도 안타다.

실측 (`inPlay:true` 로 2만 타석):

| 품질 q | 옛 타율 | 새 타율 | 장타% |
|---|---|---|---|
| 0.20 (겨우 갖다 댐) | — | **.147** | 3.5 |
| 0.24 (빗맞음) | .522 | **.197** | — |
| 0.47 (구석) | .637 | .590 | 21.8 |
| 0.59 (한가운데) | .670 | .670 | 24.8 |
| 0.75 (제대로) | .702 | .702 | 26.8 |

**잘 맞은 쪽(q≥0.5)은 한 글자도 안 건드렸다.** 거긴 이미 균형이 맞은 값이라
건드리면 전 리그 타율이 흔들린다. 못 맞은 쪽만 거듭제곱으로 떨어뜨렸다.

```js
out.babip = q<0.5 ? Math.max(0.22, Math.pow(q/0.5, 1.6))
                  : 1+(q-0.5)*0.30;      // ← 이 줄은 그대로
```

그리고 존 밖 공은 **58% 가 파울**로 빠지게 했다(구석 34% · 한가운데 22%).
유인구 한 개당 최종 인플레이 확률은 6.5% 이고 그중 타율이 .20 이니,
**유인구 한 개당 안타는 1.3%** 다. 이제 던질 이유가 있다.

**재는 법**: 소스 문자열이 아니라 **타율을 직접 굴려서** 잰다.
「빗맞음 < 보통 < 정타」 순서와 「빗맞음이 .35 밑」 두 가지를 본다(`hrtest.js`).
「위쪽 곡선을 안 건드렸다」 도 숫자로 잰다 — q=0.5/0.6/0.75/0.9/1.0 에서
옛 식과 값이 같은지.

### 캘리브레이션은 이 경로를 안 지난다

`verify.js` 는 `simGame` 을 자동으로 돌린다. `_contact` 는 **손으로 친 타석**에서만
세워지므로 위 변경은 6개 지표에 전혀 안 닿는다(실제로 바뀌기 전후 값이 동일했다).
직접 플레이 쪽 숫자를 만질 때는 `verify.js` 가 안 잡아준다 — 따로 재야 한다.

### 화면 문구가 사실과 다르면 그것도 버그다 (2.83.0)

`advtest` 의 「사기 변동」 이 오래 실패하고 있었다. 메모리에는 '사실상 상시 실패'
로 적혀 있었다. 열어보니 코드 버그가 아니라 **사기 천장**이었다 —
시즌을 잘 보내고 오면 사기가 이미 100 이라 「더 열심히 하겠다(+6)」 를 눌러도
`Math.min(100, 100+6)` 이 100 이라 숫자가 안 움직인다.

테스트는 오를 자리를 두고(70) 재게 고쳤다. 그런데 **화면 쪽에도 문제가 있었다** —
아무 일도 안 일어났는데 로그에는 「내 사기 +6」 이라고 찍히고 있었다.
이제 실제로 변한 만큼 적고, 꽉 찼으면 「사기가 더 오를 데가 없다」 고 말해준다.

**교훈**: 오래 실패하던 테스트를 열면 셋 중 하나다 —
코드가 틀렸거나(posFit), 테스트가 뒤처졌거나(whatiftest), **둘 다 조금씩**(여기).
